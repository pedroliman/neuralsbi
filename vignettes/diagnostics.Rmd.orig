---
title: "Checking the posterior"
description: >
  Validate a neural posterior in R with simulation-based calibration (SBC),
  expected coverage, TARP, and posterior predictive checks before you trust its
  credible intervals.
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Checking the posterior}
  %\VignetteEngine{knitr::rmarkdown_notangle}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
# This vignette is precomputed: see vignettes/precompute.R. Edit the .Rmd.orig,
# re-run the precompute script, and commit the regenerated .Rmd and figures.
knitr::opts_chunk$set(
  collapse = TRUE, comment = "#>", error = FALSE,
  fig.path = "figures/diagnostics-", fig.width = 5, fig.height = 4,
  dpi = 96, fig.retina = NULL, dev = "svglite"
)
set.seed(1)

# Baked with a future plan, so the simulator runs across cores. Nothing about
# the results depends on it: every simulation draws from its own RNG stream,
# so the same seed gives the same answer on 1 core or 32. See ?nsbi_parallel.
if (requireNamespace("future", quietly = TRUE)) {
  future::plan(future::multisession)
}
```

An NPE posterior is an approximation, and a bad approximation gives you no warning. It still returns draws, means and credible intervals; they are just wrong. So checking the fit is part of the workflow rather than an extra. This article walks through the package's diagnostics in the order we use them.

We use the linear-Gaussian benchmark task throughout, because it has an analytic posterior we can compare against at the end.

```{r}
library(neuralsbi)

task <- task_gaussian_linear()
task

fit <- npe(task$prior, task$simulator, n_simulations = 3000,
           density_estimator = "linear_gaussian", seed = 1)
```

## Simulation-based calibration

Simulation-based calibration (SBC) checks the posterior on average over the prior, and it needs no reference posterior to compare against. Draw $\theta$ from the prior, simulate $x$ from it, then rank that known $\theta$ among posterior draws given $x$. A calibrated posterior puts the truth anywhere in the ranking with equal probability, so the ranks come out uniform.

```{r fig.alt = "SBC rank histogram for the first parameter; flat means calibrated."}
res <- sbc(fit, task$simulator, n_sbc = 100, n_posterior_samples = 200,
           seed = 2)
res

plot_sbc(res, param = 1)
```

Read the rank histogram like this:

- Flat: calibrated.
- U-shaped: the posterior is too narrow, so it is overconfident. This is the common failure, and more simulations or longer training usually fix it.
- Hump-shaped: too wide, so it is underconfident.
- Sloped: biased in one direction.

## Expected coverage

The same ranks give a coverage curve. How often does the $p$% credible interval actually contain the truth?

```{r fig.alt = "Expected-coverage curve; points on the diagonal indicate calibration."}
expected_coverage(res)
plot_coverage(res)
```

A calibrated posterior hugs the diagonal. Points below it mean the intervals are too narrow: a 90% interval that covers the truth 70% of the time.

## TARP, a sharper coverage test

SBC looks at each parameter's marginal one at a time. TARP (Lemos et al., 2023) tests coverage of the joint posterior using random reference points, so it can catch miscalibration that per-parameter ranks miss.

```{r fig.alt = "TARP expected-coverage-probability curve on the diagonal."}
tr <- tarp(fit, task$simulator, n_tarp = 100, n_posterior_samples = 200,
           seed = 3)
tr
plot_tarp(tr)   # ECP curve on the diagonal = calibrated
```

## Posterior predictive checks

Calibration is an average over many simulated data sets. A predictive check asks about the observation you actually have. Push posterior draws back through the simulator and see whether the observed data look typical of what comes out.

```{r fig.alt = "Posterior-predictive histograms with the observed value marked."}
theta_true <- sample_prior(task$prior, 1)
x_obs <- task$simulator(drop(theta_true))

post <- posterior(fit, x_obs = x_obs)
pred <- posterior_predictive(post, task$simulator, n = 500)
plot_posterior_predictive(pred, x_obs)
```

If the observation falls in the tails of the predictive distribution, either the fit is poor for this $x$ or the simulator cannot reproduce the data at any parameter value. The second is model misspecification, and no calibration check will tell you about it.

## Comparing against a reference posterior

When a reference posterior exists, whether analytic, from MCMC, or from a long-run fit you trust, a classifier two-sample test gives you one number. A classifier that cannot tell estimated draws from reference draws scores near 0.5, and that means the posteriors match.

```{r}
draws     <- sample(post, 5000)
reference <- task$reference_posterior(x_obs, n = 5000)

c2st(draws, reference)$accuracy   # ~0.5: indistinguishable from the exact posterior
```

As a rule of thumb, accuracies below about 0.55 to 0.6 indicate a close match, and values near 1.0 mean the estimated posterior is badly off.

## The routine we suggest

1. Run `sbc()` after every fit and look at `plot_sbc()` and `plot_coverage()`. It is cheap next to training and it catches overconfidence early.
2. Run a posterior predictive check for the actual observation before drawing any conclusion from the posterior.
3. When you change estimator or training settings, compare old and new draws with `c2st()` to see whether the change moved the posterior at all.

`vignette("sir-epidemic")` runs this routine end to end on an applied problem.
