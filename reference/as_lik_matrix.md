# Coerce one of `log_lik()`'s two matrix arguments, naming it if it is wrong

Coerce one of
[`log_lik()`](https://neuralsbi.pedrodelima.com/reference/log_lik.md)'s
two matrix arguments, naming it if it is wrong

## Usage

``` r
as_lik_matrix(value, d, arg, what)
```
