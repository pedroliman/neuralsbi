# The full NSF: stacked spline-autoregressive transforms with order reversal

The full NSF: stacked spline-autoregressive transforms with order
reversal

## Usage

``` r
nsf_module(
  dim_x,
  dim_theta,
  n_transforms,
  hidden,
  n_bins,
  tail_bound,
  embedding = NULL
)
```
