# Per-row mixture log density (in standardized theta space), as a torch tensor. `theta`: (b, p) tensor, `x`: (b, q) tensor.

Per-row mixture log density (in standardized theta space), as a torch
tensor. `theta`: (b, p) tensor, `x`: (b, q) tensor.

## Usage

``` r
mdn_log_prob_tensor(net, theta, x)
```
