# Log-density of a chunk of observations under each row's mixture

Log-density of a chunk of observations under each row's mixture

## Usage

``` r
mdn_chunk_lp(mix, xs, p)
```

## Value

A `(n_theta, n_chunk)` tensor.
