# One MADE block: (theta, x) -\> per-dimension shift mu and log-scale alpha

One MADE block: (theta, x) -\> per-dimension shift mu and log-scale
alpha

## Usage

``` r
made_module(dim_x, dim_theta, hidden)
```
