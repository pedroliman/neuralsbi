# The name to record for an estimator the caller chose by string

A caller can also pass their own fitting function, which has no name
worth printing.

## Usage

``` r
estimator_label(estimator)
```

## Arguments

- estimator:

  The `density_estimator` or `classifier` argument.
