# Name one parameter in an error message, by name where there is one

Name one parameter in an error message, by name where there is one

## Usage

``` r
param_label(param_names, j)
```

## Arguments

- param_names:

  Parameter names, or `NULL`.

- j:

  Parameter index.
