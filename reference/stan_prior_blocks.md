# The prior's contribution to each Stan block

The prior's contribution to each Stan block

## Usage

``` r
stan_prior_blocks(prior, Q)
```

## Arguments

- prior:

  The fit's prior.

- Q:

  Number of parameters.

## Value

A list of four strings, one per block, each newline-terminated or empty.
