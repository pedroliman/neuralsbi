# One line per marginal, for print.nsbi_prior()

Reads back as close to the constructor call as one line allows:
`beta(2, 15) on [0, 1]`, with the bounds shown only when they are not
the family's own.

## Usage

``` r
describe_marginals(marginals)
```

## Arguments

- marginals:

  The marginals to describe.
