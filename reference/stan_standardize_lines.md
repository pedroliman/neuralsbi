# Standardization preamble shared by every generated entry point

Standardization preamble shared by every generated entry point

## Usage

``` r
stan_standardize_lines(fit, target = "x", cond = "theta")
```
