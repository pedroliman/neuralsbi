# A `<lower=, upper=>` clause, or nothing when neither side is finite

A `<lower=, upper=>` clause, or nothing when neither side is finite

## Usage

``` r
stan_bounds(lower, upper)
```

## Arguments

- lower, upper:

  The bounds.
