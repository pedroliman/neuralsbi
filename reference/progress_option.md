# Resolve the `neuralsbi.progress` option to one of "auto", "on", "off", "builtin", "progressr"

Resolve the `neuralsbi.progress` option to one of "auto", "on", "off",
"builtin", "progressr"

## Usage

``` r
progress_option()
```
