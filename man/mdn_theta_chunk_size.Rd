% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{mdn_theta_chunk_size}
\alias{mdn_theta_chunk_size}
\title{How many rows of \code{theta} to run through the MLP/Cholesky assembly at once}
\usage{
mdn_theta_chunk_size(n_theta, n_obs, max_batch)
}
\description{
Mirrors the theta-blocking \code{\link[=cross_iid]{cross_iid()}} does for the flow estimators, so
an MDN bounds the \verb{(theta, x)} pair count the same way: \code{\link[=mdn_mixture]{mdn_mixture()}}
materializes a \verb{(theta_chunk, K, dim_theta, dim_theta)} tensor for one
block, and \code{theta_chunk * n_obs <= max_batch} keeps that bounded
regardless of which of \code{theta} or \code{x} is the large dimension (#240).
}
\keyword{internal}
