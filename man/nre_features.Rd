% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre.R
\name{nre_features}
\alias{nre_features}
\title{Quadratic feature basis for the logistic ratio estimator}
\usage{
nre_features(theta, x)
}
\description{
\verb{[1, z, vech(z z')]} for \verb{z = (theta, x)}. The basis is chosen so the
estimator is \emph{exact} for a linear-Gaussian simulator: there
\eqn{\log p(x \mid \theta)} is a quadratic form in \eqn{(\theta, x)}, so the
log ratio's parameter dependence lies inside this span and the fit is
limited only by estimation error (see \code{\link[=fit_logistic_ratio]{fit_logistic_ratio()}} for why the
evidence term does not spoil that). It is the regression oracle for \code{\link[=nre]{nre()}}
that \code{"linear_gaussian"} is for \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}}.
}
\details{
It costs \code{1 + d + d(d+1)/2} columns for \code{d = dim_theta + dim_x}, so it is a
baseline for small models, not a substitute for a neural classifier on wide
data.
}
\keyword{internal}
