% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nle_posterior.R
\name{resolve_x_iid}
\alias{resolve_x_iid}
\title{All rows of the observation, unlike \code{resolve_x()} which keeps only the first}
\usage{
resolve_x_iid(post, x, arg = "obs")
}
\arguments{
\item{post}{An \code{nsbi_mcmc_posterior} object.}

\item{x}{Observation to condition on, or \code{NULL} to use the posterior's
\code{x_obs}.}

\item{arg}{Name the caller's argument goes by, for the error message.
\code{\link[=sample]{sample()}} calls it \code{obs} and \code{\link[=log_prob]{log_prob()}} calls it \code{x}.}
}
\description{
Thin wrapper around \code{\link[=resolve_obs]{resolve_obs()}} with \code{first_row = FALSE}; see there for
the rationale shared with \code{\link[=resolve_x]{resolve_x()}}, including why a non-finite entry
stops here rather than warns.
}
\keyword{internal}
