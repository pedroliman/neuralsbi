% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre_posterior.R
\name{sample.nsbi_nre_posterior}
\alias{sample.nsbi_nre_posterior}
\title{Sample an NRE posterior with MCMC}
\usage{
\method{sample}{nsbi_nre_posterior}(
  x,
  size = 1000,
  n = size,
  obs = NULL,
  refresh = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{An \code{nsbi_nre_posterior} object (named \code{x} to satisfy the \code{\link[=sample]{sample()}}
generic).}

\item{size, n}{Number of posterior draws (\code{n} is an alias for \code{size}).}

\item{obs}{Observation to condition on (defaults to the posterior's \code{x_obs}).}

\item{refresh}{Force a new run even when a cached one would do.}

\item{verbose}{Report sampling progress.}

\item{...}{Unused.}
}
\value{
An \verb{n x dim} matrix of posterior draws (class \code{nsbi_samples}), with
convergence diagnostics attached as attribute \code{diagnostics}.
}
\description{
Identical in behavior to \code{\link[=sample.nsbi_nle_posterior]{sample.nsbi_nle_posterior()}}, including the draw
cache: asking for the same or fewer draws from the same observation returns
the cached run instead of starting a new chain.
}
