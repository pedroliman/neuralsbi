% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_estimator.R
\name{lingauss_mean}
\alias{lingauss_mean}
\title{Conditional mean of the linear-Gaussian estimator}
\usage{
lingauss_mean(de, x)
}
\arguments{
\item{de}{A fitted \code{nsbi_de_lingauss} object.}

\item{x}{The conditioning variable: a numeric vector, matrix or data frame
with \code{de$dim_x} columns.}
}
\value{
An \code{nrow(x) x de$dim_theta} matrix of conditional means.
}
\description{
\code{lingauss_mean()} is the only place \code{x} meets \code{de$B}, so it is also the
place the width of \code{x} is checked. It passes \code{de$dim_x} to
\code{\link[=as_theta_matrix]{as_theta_matrix()}} for the reason every neural estimator passes its own: a
wrong-width \code{x} otherwise gets as far as the matrix product and is reported
as "non-conformable arguments", which names neither the argument nor the
width expected of it. An estimator fitted before \code{dim_x} was recorded has
\code{NULL} here and keeps the old unchecked behaviour.
}
\keyword{internal}
