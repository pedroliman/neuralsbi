% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{likelihood_fn}
\alias{likelihood_fn}
\title{A surrogate likelihood as a plain R function}
\usage{
likelihood_fn(fit, x_obs, ...)
}
\arguments{
\item{fit}{An \code{nsbi_nle} fit from \code{\link[=nle]{nle()}}.}

\item{x_obs}{The observation to condition on. Rows are independent
observations; the log-likelihood sums over them.}

\item{...}{Passed to \code{\link[=log_lik]{log_lik()}}.}
}
\value{
A function of \code{theta} returning a numeric vector of
log-likelihoods, with the observation attached as attribute \code{x_obs}.
}
\description{
Fixes an observation and returns \verb{function(theta)}, so the learned
likelihood can be handed to anything in R that wants a log-density:
\code{\link[stats:optim]{stats::optim()}}, an MCMC package, an importance sampler, a profile
likelihood. The returned function is vectorized -- pass a matrix of
parameter values and get one log-likelihood per row.
}
\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nle(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 1000, density_estimator = "linear_gaussian")

loglik <- likelihood_fn(fit, matrix(rnorm(20, 1, 0.5), ncol = 1))
optimize(function(m) loglik(m), c(-3, 3), maximum = TRUE)$maximum
}
