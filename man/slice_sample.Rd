% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{slice_sample}
\alias{slice_sample}
\title{Vectorized univariate slice sampler}
\usage{
slice_sample(
  log_prob_fn,
  init,
  n_draws,
  warmup = 200L,
  thin = 10L,
  width = 1,
  max_steps = 100L,
  verbose = FALSE
)
}
\arguments{
\item{log_prob_fn}{Vectorized log-density: takes an \verb{m x dim} matrix, returns
\code{m} log-densities (\code{-Inf} allowed).}

\item{init}{\verb{n_chains x dim} matrix of starting points, all with finite
log-density.}

\item{n_draws}{Number of retained draws in total, across all chains.}

\item{warmup}{Steps discarded at the start of each chain.}

\item{thin}{Keep one draw in \code{thin}.}

\item{width}{Initial slice width per dimension (recycled). Adapted during
warmup and then held fixed.}

\item{max_steps}{Cap on stepping-out expansions per coordinate.}

\item{verbose}{Report progress.}
}
\value{
A list with \code{draws} (\verb{n_draws x dim}), \code{chains} (\verb{n_kept x n_chains x dim} array) and \code{n_evals}.
}
\description{
Vectorized univariate slice sampler
}
\keyword{internal}
