% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_index}
\alias{check_index}
\title{Validate a column index, which may be given as a name}
\usage{
check_index(value, arg, nms = NULL, n, what = "parameter")
}
\arguments{
\item{value}{The user's value: one index, or one name to match against \code{nms}.}

\item{arg}{Name of the argument.}

\item{nms}{Names to match a character value against, or \code{NULL} when the
columns are unnamed.}

\item{n}{Number of columns.}

\item{what}{Noun for one column, e.g. \code{"parameter"}.}
}
\value{
An integer index between 1 and \code{n}.
}
\description{
\code{plot_sbc(sbc_result, param = 99)} used to reach \code{ranks[, 99]} and report
"subscript out of bounds", which names neither the argument nor how many
parameters there are. The rank matrix carries \code{colnames} and every other
plotting function labels by name, so a name is accepted here too and
resolved against \code{nms}.
}
\keyword{internal}
