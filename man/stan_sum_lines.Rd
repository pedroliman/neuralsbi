% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan.R
\name{stan_sum_lines}
\alias{stan_sum_lines}
\title{The i.i.d.-sum entry point shared by every generated \verb{_sum_lpdf}}
\usage{
stan_sum_lines(fit, P, body, precompute = "")
}
\description{
Standardizes \code{theta}, declares \code{x}'s center/scale, and accumulates \code{body}
(an expression for one observation's log density, in terms of \code{x[n]}) over
\code{rows(x)} before applying the jacobian once at the end. \code{precompute} is the
one place estimators differ: \code{linear_gaussian} and the MDN can build their
conditional distribution once, outside the loop, because it depends on
\code{theta} alone; MAF has nothing to hoist, so its caller leaves this at the
default. Mirrors \code{\link[=de_log_lik_iid]{de_log_lik_iid()}} (R/likelihood.R), which sums the same
per-observation log density on the R side; the two have to agree.
}
\keyword{internal}
