% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{log_lik}
\alias{log_lik}
\alias{log_lik.nsbi_nle}
\title{Evaluate a surrogate likelihood}
\usage{
log_lik(fit, theta, x, ...)

\method{log_lik}{nsbi_nle}(fit, theta, x, sum_iid = TRUE, max_batch = 1e+05, ...)
}
\arguments{
\item{fit}{An \code{nsbi_nle} fit from \code{\link[=nle]{nle()}}.}

\item{theta}{Parameter values: a numeric vector (one parameter set) or an
\verb{n_theta x dim_theta} matrix.}

\item{x}{Observed data: a numeric vector (one observation) or an
\verb{n_obs x dim_x} matrix whose rows are independent observations.}

\item{...}{Unused, for S3 consistency.}

\item{sum_iid}{Sum the log-density over the rows of \code{x} (the default). Set
\code{FALSE} to get the per-observation values instead.}

\item{max_batch}{Largest number of \verb{(theta, x)} pairs evaluated in one call
to the estimator. Only affects memory and speed.}
}
\value{
With \code{sum_iid = TRUE}, a numeric vector with one entry per row of
\code{theta}. With \code{sum_iid = FALSE}, an \verb{n_theta x n_obs} matrix.
}
\description{
\code{log_lik()} evaluates the likelihood learned by \code{\link[=nle]{nle()}}:
\eqn{\log q_\phi(x \mid \theta)}, in the original units of both.
}
\details{
Rows of \code{x} are treated as \strong{independent observations from the same
parameter}, so by default the result sums over them,
\eqn{\sum_i \log q_\phi(x_i \mid \theta)}. That sum is the whole point of
NLE: the estimator is trained on one observation at a time, and the number
of observations you condition on afterwards is free.
}
\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nle(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 1000, density_estimator = "linear_gaussian")

x_obs <- matrix(rnorm(20, mean = 1, sd = 0.5), ncol = 1)
grid <- matrix(seq(-2, 2, length.out = 5), ncol = 1)
log_lik(fit, grid, x_obs)
}
\seealso{
\code{\link[=likelihood_fn]{likelihood_fn()}} for a closure over a fixed observation,
\code{\link[=posterior]{posterior()}} to turn the likelihood into posterior draws.
}
