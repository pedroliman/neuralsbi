% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre.R
\name{nre}
\alias{nre}
\title{Neural Ratio Estimation (NRE)}
\usage{
nre(
  prior,
  simulator = NULL,
  n_simulations = 1000,
  sim_args = list(),
  theta = NULL,
  x = NULL,
  classifier = c("resnet", "mlp", "linear", "logistic"),
  num_atoms = 10L,
  hidden = 50L,
  n_blocks = 2L,
  embedding_net = NULL,
  max_epochs = 2000L,
  batch_size = 200L,
  lr = 5e-04,
  validation_fraction = 0.1,
  patience = 20L,
  n_restarts = 1L,
  clip_grad_norm = 5,
  standardize = TRUE,
  device = "cpu",
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{prior}{An \code{nsbi_prior} (see \code{\link[=prior_uniform]{prior_uniform()}}, \code{\link[=prior_normal]{prior_normal()}}).}

\item{simulator}{A function called once per parameter set, returning one
simulated observation: a numeric vector, a scalar, or a one-row matrix or
data frame. Parameters arrive either as named arguments (when the prior's
names match the simulator's formals) or as one named vector. Names on the
output become the outcome names used in plots. See \link{nsbi_simulator}.
Ignored if \code{theta} and \code{x} are given.}

\item{n_simulations}{Number of prior draws to simulate when \code{simulator} is
used and \code{theta}/\code{x} are not supplied.}

\item{sim_args}{Named list of extra arguments passed to every simulator
call: observed data, a time grid, a fixed population size, solver
settings. See \link{nsbi_simulator}.}

\item{theta, x}{Optional pre-computed simulations. If supplied, \code{simulator}
and \code{n_simulations} are ignored. Column names on \code{theta} (or names on
\code{prior}'s \code{mean}/\code{low}) and on \code{x} are carried through to posterior
samples, SBC results, and their plots.}

\item{classifier}{One of \code{"resnet"} (a residual MLP, needs \code{torch}; the
default, matching Python \code{sbi}), \code{"mlp"} (a plain MLP, needs \code{torch}),
\code{"linear"} (a single linear layer on the raw inputs, needs \code{torch}), or
\code{"logistic"} (a closed-form logistic regression on quadratic features, no
\code{torch}), or a function \verb{function(theta, x)} returning a fitted ratio
estimator -- one whose class has a \code{de_log_ratio()} method, which is the
only thing the rest of the pipeline asks of it.}

\item{num_atoms}{Number of parameter values the classifier compares per
simulation: one true and \code{num_atoms - 1} contrasts. Clamped to the
minibatch size, as in \code{sbi}.}

\item{hidden}{Width of the classifier's hidden layers. One number, not a
per-layer vector as in \code{\link[=npe]{npe()}}: \code{sbi}'s classifiers use a single width
throughout, and \code{n_blocks} is what sets the depth.}

\item{n_blocks}{Depth of the classifier: residual blocks for \code{"resnet"},
hidden layers for \code{"mlp"}. Ignored by \code{"linear"} and \code{"logistic"}.
\code{sbi} fixes its MLP at two hidden layers and only lets \code{num_blocks} reach
the residual net; here the one argument sets both.}

\item{embedding_net}{Optional summary network built with \code{\link[=embedding_mlp]{embedding_mlp()}}.
The classifier then sees \eqn{(\theta, f_\psi(x))}, with the embedding
trained jointly. Ignored (with a warning) by \code{"logistic"}.}

\item{max_epochs, batch_size, lr, validation_fraction, patience}{Neural training
controls (Adam optimizer, early stopping on validation loss). The defaults
(\code{batch_size = 200}, \code{lr = 5e-4}, \code{validation_fraction = 0.1},
\code{patience = 20}) match Python \code{sbi}; \code{max_epochs} is a high guard cap that
early stopping normally reaches first.}

\item{n_restarts}{Train this many independently initialized networks and keep
the one with the best validation loss (guards against bad initializations
and MDN mode collapse).}

\item{clip_grad_norm}{Maximum gradient norm during training (\code{Inf} disables
clipping). The learning rate also decays 2x after 10 epochs without
validation improvement.}

\item{standardize}{Whether to z-score \code{theta} and \code{x} before training
(strongly recommended; default \code{TRUE}).}

\item{device}{Where to train the neural estimator: \code{"cpu"} (the default),
\code{"cuda"}, \code{"mps"}, or \code{"gpu"}/\code{"auto"} to resolve CUDA -> MPS -> CPU
(mirroring Python \code{sbi}'s \code{"gpu"}). CPU is the default on purpose --
matching \code{sbi}, not auto-selecting a GPU -- and \code{"cuda"}/\code{"mps"} error if
the requested device is not actually available rather than falling back
silently, so a typo or a missing driver is not mistaken for a slow CPU
run. Only \code{"gpu"}/\code{"auto"} falls back to CPU without complaint, since it
never named a specific device. Ignored (with no error) by
\code{"linear_gaussian"}, which has no GPU concept. For the small networks
typical of SBI models (the SIR example, say), \code{"mps"}/\code{"cuda"} can be
\emph{slower} than CPU -- per-kernel launch overhead dominates until the net
and batch are large -- so try CPU first and switch only if profiling
shows a win.}

\item{seed}{Optional integer seed for reproducibility.}

\item{verbose}{Print training progress.}
}
\value{
An object of class \code{nsbi_nre}. Evaluate the learned ratio with
\code{\link[=log_ratio]{log_ratio()}} or turn it into a posterior with \code{\link[=posterior]{posterior()}}.
}
\description{
\code{nre()} learns the third factorization of the joint. \code{\link[=npe]{npe()}} learns the
posterior and \code{\link[=nle]{nle()}} learns the likelihood; \code{nre()} learns neither density
but their ratio, \eqn{r(\theta, x) = p(x \mid \theta) / p(x)}, by training a
binary classifier to tell \eqn{(\theta, x)} pairs drawn from the joint apart
from pairs whose parameter came from a different simulation. The posterior
follows from Bayes' rule,
\eqn{p(\theta \mid x) \propto r(\theta, x)\,p(\theta)}, and is sampled with
MCMC by \code{\link[=posterior]{posterior()}}.
}
\section{Why a classifier instead of a density}{


A density estimator has to spend capacity describing the shape of a
distribution -- normalization, tails, the lot -- even where that shape does
not affect the posterior. A classifier only has to say which of two
parameter values explains the data better, and the optimal classifier's
logit \emph{is} the log ratio. That makes NRE the natural choice when the data
are high-dimensional or awkward to model directly (discrete counts, mixed
types, anything a flow handles badly) but easy to discriminate.

Like \code{\link[=nle]{nle()}} and unlike \code{\link[=npe]{npe()}}, the ratio is learned for a single
observation, so the log-likelihood of \eqn{n} independent trials from the
same parameter is a sum:

\deqn{\log \frac{p(x_1, \ldots, x_n \mid \theta)}{\prod_i p(x_i)} = \sum_{i=1}^{n} \log r(\theta, x_i).}

Train once, condition on as many trials as you like. The price is the same
one \code{\link[=nle]{nle()}} pays: posterior draws cost an MCMC run rather than a forward
pass.
}

\section{The training objective}{


The default is the \emph{atomic} loss of Durkan et al. (2020), which is what
\code{sbi}'s \code{NRE} (an alias for \code{NRE_B}) trains. For each simulation
\eqn{(\theta_i, x_i)} in a minibatch, \code{num_atoms - 1} contrasting parameters
are taken from the other simulations in that batch, and the classifier is
scored on a \code{num_atoms}-way softmax over which of them produced \eqn{x_i}:

\deqn{\mathcal{L} = -\frac{1}{b}\sum_{i} \left[ f(\theta_i, x_i) - \log \sum_{k} \exp f(\theta_{ik}, x_i) \right].}

More atoms mean a harder discrimination problem and a sharper ratio, at a
linear cost in forward passes per epoch. \code{num_atoms = 10} is \code{sbi}'s
default and this one. The softmax is invariant to adding any function of
\eqn{x} to \eqn{f}, so the learned ratio is calibrated up to a constant at
each fixed observation -- exactly what a posterior needs, and the reason
\code{\link[=log_prob]{log_prob()}} on an NRE posterior is unnormalized.
}

\section{Standardization has no Jacobian here}{


The estimators in \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}} train on z-scored data and need a
change-of-variables term to report densities in the original units. A ratio
needs none: both \eqn{p(x \mid \theta)} and \eqn{p(x)} pick up the same
Jacobian factor and it cancels, so \code{log_ratio()} in standardized space is
already the ratio in the units the simulator returned.
}

\examples{
# One noisy measurement per simulator call; the observation is 50 of them.
prior <- prior_uniform(c(mu = -3), c(mu = 3))
simulator <- function(mu) c(y = rnorm(1, mu, 0.5))

fit <- nre(prior, simulator, n_simulations = 2000, classifier = "logistic")

x_obs <- matrix(rnorm(50, mean = 1, sd = 0.5), ncol = 1)
log_ratio(fit, theta = c(1), x = x_obs)
}
\references{
Hermans, J., Begy, V. and Louppe, G. (2020). Likelihood-free
MCMC with Amortized Approximate Ratio Estimators. \emph{ICML}.
\doi{10.48550/arXiv.1903.04057}

Durkan, C., Murray, I. and Papamakarios, G. (2020). On Contrastive
Learning for Likelihood-free Inference. \emph{ICML}.
\doi{10.48550/arXiv.2002.03712}
}
\seealso{
\code{\link[=log_ratio]{log_ratio()}} to evaluate the ratio, \code{\link[=posterior]{posterior()}} to sample it,
\code{\link[=nle]{nle()}} for the likelihood factorization and \code{\link[=npe]{npe()}} for the posterior
one.
}
