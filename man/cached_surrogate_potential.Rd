% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nle_posterior.R
\name{cached_surrogate_potential}
\alias{cached_surrogate_potential}
\title{Get (or build and cache) the surrogate potential behind an MCMC posterior}
\usage{
cached_surrogate_potential(post, x_obs, max_batch)
}
\arguments{
\item{post}{An \code{nsbi_mcmc_posterior} object.}

\item{x_obs}{The (already resolved) observation to condition on.}

\item{max_batch}{Largest batch size to pass to \code{\link[=surrogate_potential]{surrogate_potential()}}.}
}
\value{
\verb{function(theta)}, the cached (or freshly built) potential.
}
\description{
\code{\link[=surrogate_potential]{surrogate_potential()}} standardizes \code{x_obs} and lets the estimator
precompute whatever it can from the observation alone (see its own docs) --
work meant to be paid once per observation, not once per evaluation.
\code{\link[=slice_sample_surrogate]{slice_sample_surrogate()}} already gets this for free because it builds the
closure once and calls it for every MCMC step of a whole chain, but
\code{\link[=mcmc_log_prob]{mcmc_log_prob()}} used to rebuild the closure on every single \code{\link[=log_prob]{log_prob()}}
call. That is expensive on its own, and ruinous for \code{\link[=map_estimate]{map_estimate()}}, whose
optimizer calls \code{log_prob()} once per evaluation against the same \code{x_obs}
(#272). The closure is cached here on the posterior's cache environment and
only rebuilt when \code{x_obs} or \code{max_batch} changes.
}
\keyword{internal}
