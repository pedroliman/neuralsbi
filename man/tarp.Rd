% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{tarp}
\alias{tarp}
\title{TARP expected coverage}
\usage{
tarp(
  fit,
  simulator,
  prior = fit$prior,
  n_tarp = 200L,
  n_posterior_samples = 1000L,
  references = c("uniform", "prior"),
  sim_args = list(),
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{An \code{nsbi_npe} fit from \code{\link[=npe]{npe()}}, or an \code{nsbi_nle} fit from
\code{\link[=nle]{nle()}}. With an NLE fit every trial is a separate MCMC run, so start
with a small \code{n_sbc} and raise it once the cost is known.}

\item{simulator}{The simulator used for inference; called once per trial
(see \link{nsbi_simulator}).}

\item{prior}{The prior used for inference (defaults to \code{fit$prior}).}

\item{n_tarp}{Number of TARP trials (fresh (theta, x) pairs).}

\item{n_posterior_samples}{Posterior draws per trial.}

\item{references}{How to draw reference points: \code{"uniform"} (default, uniform
over the hyper-rectangle spanned by the true parameter draws, as in the
paper) or \code{"prior"} (draws from the prior).}

\item{sim_args}{Named list of extra arguments passed to every simulator
call; see \link{nsbi_simulator}.}

\item{seed}{Optional seed.}
}
\value{
An object of class \code{nsbi_tarp} with the per-trial coverage values
and the ECP curve. Plot it with \code{\link[=plot_tarp]{plot_tarp()}}.
}
\description{
Tests of Accuracy with Random Points (Lemos et al. 2023). For each trial a
true parameter is drawn from the prior, data are simulated, and posterior
samples are drawn conditioned on those data. Given a random reference point,
the fraction of posterior samples closer to the reference than the truth is
the credibility level of the smallest distance-based credible region that
contains the truth. For a calibrated posterior these fractions are uniform,
so the expected coverage probability (ECP) at credibility level alpha equals
alpha.
}
\details{
Unlike \code{\link[=sbc]{sbc()}}, which ranks each parameter marginally, TARP is a \emph{joint}
test: it can detect posteriors whose marginals are calibrated but whose
correlation structure is wrong. Distances are computed after z-scoring each
parameter (using the spread of the true draws), so parameters on different
scales contribute comparably.

A trial whose simulation returns non-finite output is dropped, which lowers
the effective \code{n_tarp}.
}
\references{
Lemos, Coogan, Hezaveh & Perreault-Levasseur (2023),
"Sampling-based accuracy testing of posterior estimators for general
inference", ICML. \doi{10.48550/arXiv.2302.03026}
}
