% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_function}
\alias{check_function}
\title{Validate a callback argument}
\usage{
check_function(f, arg, what = NULL, n_args = 1L)
}
\arguments{
\item{f}{The user's value.}

\item{arg}{Name of the argument.}

\item{what}{Optional phrase naming the argument the function receives, e.g.
\code{"the number of draws"}. Shown in parentheses.}

\item{n_args}{Number of positional arguments \code{f} will be called with.
Defaults to 1. \code{f} passes if it has at least \code{n_args} formals, or fewer
formals plus \code{...}.}
}
\value{
\code{f}, invisibly.
}
\description{
A function stored now and called later fails at the call site, which can be
a training run and several frames away from the argument that was wrong.
Checking at the constructor that it is a function, and that it can take the
argument it will be given, keeps the complaint next to the mistake.
}
\keyword{internal}
