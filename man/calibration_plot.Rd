% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{calibration_plot}
\alias{calibration_plot}
\title{Shared skeleton for a nominal-vs-empirical calibration plot}
\usage{
calibration_plot(df, band, xlab, ylab)
}
\arguments{
\item{df}{Base data for the plot, inherited by whatever curve layer the
caller adds afterward.}

\item{band}{A data frame with \code{nominal}, \code{lo} and \code{hi} columns (see
\code{\link[=binom_band]{binom_band()}}), shaded as the Monte-Carlo uncertainty ribbon.}

\item{xlab, ylab}{Axis labels.}
}
\value{
A \code{ggplot} object carrying the shared layers, ready for the
caller's curve, scale and title.
}
\description{
The Monte-Carlo band, the diagonal reference line, the equal-aspect
\verb{[0, 1] x [0, 1]} frame and the minimal theme are the same figure in
\code{\link[=plot_coverage]{plot_coverage()}} and \code{\link[=plot_tarp]{plot_tarp()}}; only the curve drawn on top (one per
parameter, with a legend, for \code{\link[=plot_coverage]{plot_coverage()}}; a single curve for
\code{\link[=plot_tarp]{plot_tarp()}}) and the title differ. This builds the shared part and
leaves the caller to add its own \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}} and
\code{\link[ggplot2:labs]{ggplot2::labs()}} on top.
}
\keyword{internal}
