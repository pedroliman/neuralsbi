% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{surrogate_score}
\alias{surrogate_score}
\title{The body \code{\link[=log_lik]{log_lik()}} and \code{\link[=log_ratio]{log_ratio()}} share}
\usage{
surrogate_score(
  fit,
  theta,
  x,
  sum_iid,
  max_batch,
  matrix_fn,
  evaluator,
  log_jac
)
}
\arguments{
\item{fit}{An \code{nsbi_nle} or \code{nsbi_nre} fit.}

\item{theta, x, sum_iid, max_batch}{As in \code{\link[=log_lik]{log_lik()}}.}

\item{matrix_fn}{\verb{function(de, x_z, theta_z, max_batch)} giving the
\verb{n_theta x n_obs} matrix in standardized space.}

\item{evaluator}{\verb{function(de, x_z, max_batch)} returning a \verb{function(theta)}
giving that matrix's row sums without building it.}

\item{log_jac}{Apply the \code{x} standardizer's log-Jacobian, once per
observation.}
}
\description{
Both take a \code{theta} and an \code{x} whose rows are independent observations,
check them, standardize them, and either return the \verb{n_theta x n_obs} matrix
or its row sums. The two differ in the same two places
\code{\link[=surrogate_potential]{surrogate_potential()}} does: which pair of functions scores the estimator,
and whether the standardization of \code{x} needs a change-of-variables term. A
density reported in the simulator's units needs one; a ratio does not, since
the Jacobian cancels between its numerator and denominator.
}
\keyword{internal}
