% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npe.R
\name{check_architecture}
\alias{check_architecture}
\title{Validate the architecture arguments shared by \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}}}
\usage{
check_architecture(n_components, n_transforms, hidden, n_bins, tail_bound)
}
\arguments{
\item{n_components, hidden}{MDN settings: number of mixture components
(default 10, as in \code{sbi}) and a vector of hidden-layer widths.}

\item{n_transforms}{MAF/NSF setting: number of stacked autoregressive
transforms (default 5, as in \code{sbi}).}

\item{n_bins, tail_bound}{NSF settings: number of spline bins per transform
and the half-width of the interval the spline acts on (outside it the
transform is the identity).}
}
\description{
All of them are checked whichever estimator was asked for, so \code{n_bins = 0}
is an error even under \code{"linear_gaussian"}, which ignores it. A value that
cannot build a network is a mistake in the call whether or not this run
would have read it.
}
\keyword{internal}
