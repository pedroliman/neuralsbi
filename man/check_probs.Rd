% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_probs}
\alias{check_probs}
\title{Validate a vector of probabilities}
\usage{
check_probs(p, arg, open = TRUE)
}
\arguments{
\item{p}{The user's value.}

\item{arg}{Name of the argument.}

\item{open}{Require \code{0 < p < 1} (the default). \code{FALSE} allows the endpoints.}
}
\value{
\code{p} as a double vector.
}
\description{
\code{\link[=check_prob]{check_prob()}} for an argument that is a vector rather than one number, such
as the credibility \code{levels} of \code{\link[=expected_coverage]{expected_coverage()}}. A level outside \verb{(0, 1)} produces one meaningless number in a table of plausible ones:
\code{levels = c(-1, 2)} scores the empty interval and the whole line, and comes
back as coverage 0 and 1 with nothing said. The message lists the values, as
\code{\link[=check_counts]{check_counts()}} does, since which entry is wrong is the thing worth reading.
}
\keyword{internal}
