% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{for_each_trial}
\alias{for_each_trial}
\title{Shared per-trial posterior-draw loop for sbc() and tarp()}
\usage{
for_each_trial(fit, x_all, n_posterior_samples, label, f, ...)
}
\arguments{
\item{fit}{An \code{nsbi_npe} or \code{nsbi_nle} fit.}

\item{x_all}{Matrix of simulated data, one row (one trial) per observation.}

\item{n_posterior_samples}{Posterior draws requested per trial.}

\item{label}{Progress-bar label.}

\item{f}{\verb{function(draws, i)}, the per-trial metric.}

\item{...}{Passed to \code{\link[=posterior]{posterior()}}.}
}
\value{
A list with \code{results} (a list of length \code{nrow(x_all)}, one \code{f()}
return value per trial) and \code{n_posterior_samples} (the draw count trials
were actually scored against).
}
\description{
Draws a posterior for each row of \code{x_all}, insists on the full
\code{n_posterior_samples} count via \code{\link[=diagnostic_draws]{diagnostic_draws()}}, and hands the result
to \code{f(draws, i)} for whichever per-trial metric the caller wants: \code{sbc()}
returns a rank row, \code{tarp()} a scalar coverage value.
}
\details{
The denominator a caller bins or normalizes against (\code{sbc()}'s rank scale,
via the p-value bins and \code{\link[=expected_coverage]{expected_coverage()}}) is read back from how many
draws a trial actually returned, not trusted from the \code{n_posterior_samples}
argument -- so if a future change ever lets a short draw through instead of
erroring, the diagnostic is still scored on the scale it was actually drawn
on rather than the one it was asked for.

\code{finally}, not \code{on.exit()}: this block runs inside
\code{progressr::with_progress()}, so \code{on.exit()} would attach to the promise's
forcing frame and close the bar before the loop starts (see
\code{run_simulator()} in R/parallel.R).
}
\keyword{internal}
