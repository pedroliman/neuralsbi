% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{binom_band}
\alias{binom_band}
\title{99\% Monte-Carlo binomial band for a calibration curve}
\usage{
binom_band(nominal, n, level = 0.99)
}
\arguments{
\item{nominal}{Nominal level(s) the band is evaluated at.}

\item{n}{Number of trials the empirical coverage was computed over.}

\item{level}{Two-sided coverage of the band, e.g. \code{0.99} for the
\code{c(0.005, 0.995)} binomial quantiles.}
}
\value{
A data frame with columns \code{nominal}, \code{lo} and \code{hi}, all as
fractions of \code{n}.
}
\description{
At each nominal level, how far empirical coverage over \code{n} trials can
wander from the diagonal by chance alone, under a \code{Binomial(n, nominal)}
model of the count that lands inside its interval. \code{\link[=plot_coverage]{plot_coverage()}} and
\code{\link[=plot_tarp]{plot_tarp()}} shade this as the ribbon behind their curve; \code{\link[=plot_sbc]{plot_sbc()}}
uses the same interval, scaled back up to a count, for its histogram's
dashed reference lines. One formula in one place means all three figures'
bands agree on what "real" departure from calibration looks like.
}
\keyword{internal}
