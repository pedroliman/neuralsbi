% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{resolve_obs}
\alias{resolve_obs}
\title{Resolve the observation a posterior conditions on}
\usage{
resolve_obs(post, x, first_row, arg = if (first_row) "x" else "obs")
}
\arguments{
\item{post}{An \code{nsbi_posterior} object.}

\item{x}{Observation to condition on, or \code{NULL} to use the posterior's
\code{x_obs}.}

\item{first_row}{\code{TRUE} to keep row 1 only, with a warning when there was
more than one row (the \code{\link[=resolve_x]{resolve_x()}} behavior); \code{FALSE} to keep every row
(the \code{\link[=resolve_x_iid]{resolve_x_iid()}} behavior).}

\item{arg}{Name the caller's argument goes by, for \code{check_numeric()}'s and
\code{check_finite()}'s error messages. Defaults to \code{"x"} when \code{first_row} and
\code{"obs"} otherwise, matching \code{\link[=sample]{sample()}}'s and \code{\link[=log_prob]{log_prob()}}'s parameter
names; the "no observation supplied" message is fixed to the same names
and does not vary with \code{arg}.}
}
\value{
A matrix with \code{dim_x} columns: one row if \code{first_row}, every row
otherwise.
}
\description{
\code{resolve_x()} and \code{resolve_x_iid()} are both thin wrappers around this: an
NPE fit maps one observation to one posterior, so \code{resolve_x()} calls this
with \code{first_row = TRUE} and keeps only row 1; an NLE fit's log-likelihood
sums over rows as independent observations of the same parameter, so
\code{resolve_x_iid()} calls this with \code{first_row = FALSE} and keeps every row.
The same \code{x_obs} therefore means "the first observation" to \code{\link[=npe]{npe()}} and
"200 observations" to \code{\link[=nle]{nle()}}, and a user moving a working call from one to
the other would otherwise get a posterior conditioned on a single data
point with nothing said about it. Truncating to row 1 warns rather than
fails: taking row 1 of a simulation matrix is a reasonable thing to ask
for.
}
\details{
A non-finite entry is a different matter and stops here regardless of
\code{first_row}. An \code{NA} passes through \code{apply_standardizer()} into
\code{de_sample()} (NPE) or into \code{\link[=mcmc_init]{mcmc_init()}} (NLE) and comes back as
unusable output with nothing to say about the observation -- for NPE the
complaint lands in \code{stats::quantile()} inside \code{summary()}, for NLE in
\code{mcmc_init()} complaining about initialization. There is nothing sensible
to condition on either way, so this errors rather than warns.
}
\keyword{internal}
