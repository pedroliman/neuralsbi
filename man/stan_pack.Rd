% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan.R
\name{stan_pack}
\alias{stan_pack}
\title{Flatten the trained weights into one vector, recording where each block sits}
\usage{
stan_pack(fit)
}
\description{
Stan has no closures and no struct types, so everything the generated
function needs arrives as a single \verb{vector w} and is sliced back out inside.
Matrices are flattened column-major, which is exactly what Stan's
\code{to_matrix()} expects, so the two sides agree without any transposition.
}
\keyword{internal}
