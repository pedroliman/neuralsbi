% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{fit_summary}
\alias{fit_summary}
\title{Print a fit and return its training metadata invisibly}
\usage{
fit_summary(object, estimator)
}
\arguments{
\item{object}{The fit.}

\item{estimator}{A one-element named list, spliced in ahead of the rest.}
}
\description{
The body every \code{summary()} method for a fit shares. \code{estimator} is the one
field whose name depends on what was trained: \code{density_estimator} for
\code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}}, \code{classifier} for \code{\link[=nre]{nre()}}. \code{print()} inside dispatches
on the object's own class, so nothing else here has to know which fit it has.
}
\keyword{internal}
