% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{posterior}
\alias{posterior}
\alias{posterior.default}
\alias{posterior.nsbi_npe}
\title{Posterior objects}
\usage{
posterior(fit, x_obs = NULL, ...)

\method{posterior}{default}(fit, x_obs = NULL, ...)

\method{posterior}{nsbi_npe}(fit, x_obs = NULL, ...)
}
\arguments{
\item{fit}{An \code{nsbi_npe} object from \code{\link[=npe]{npe()}} or \code{\link[=npe_sequential]{npe_sequential()}}, an
\code{nsbi_nle} object from \code{\link[=nle]{nle()}}, or an \code{nsbi_nre} object from \code{\link[=nre]{nre()}}.}

\item{x_obs}{Optional default observation to condition on. If supplied it
becomes the default \code{x} for \code{\link[=sample]{sample()}}, \code{\link[=log_prob]{log_prob()}} and \code{\link[=map_estimate]{map_estimate()}}.
For an NLE or NRE fit, rows of \code{x_obs} are independent observations.}

\item{...}{Passed to methods. See \code{\link[=posterior.nsbi_nle]{posterior.nsbi_nle()}} and
\code{\link[=posterior.nsbi_nre]{posterior.nsbi_nre()}} for the MCMC controls those fits accept.}
}
\value{
An \code{nsbi_posterior} object.
}
\description{
A posterior wraps a trained \code{\link[=npe]{npe()}} fit together with (optionally) a default
observation \code{x_obs}. It knows how to draw posterior samples, evaluate the
posterior log-density, and find the maximum-a-posteriori (MAP) estimate. All
transforms between standardized training space and the original parameter
space are handled internally.

The three inference methods reach a posterior by different routes, and
\code{posterior()} hides the difference. An \code{\link[=npe]{npe()}} fit already \emph{is} a posterior
estimator, so the returned object samples with a forward pass. An \code{\link[=nle]{nle()}}
fit only knows the likelihood and an \code{\link[=nre]{nre()}} fit only the likelihood ratio,
so both return an object that samples with MCMC and takes the extra
arguments that implies.
}
\details{
For bounded priors, samples that fall outside the prior support are rejected
("leakage" correction), and \code{\link[=log_prob]{log_prob()}} is renormalized by the estimated
acceptance probability so it integrates to one over the support.
}
\seealso{
\code{\link[=save_npe]{save_npe()}}, which is how a torch-backed fit gets to disk and
back; \code{readRDS()} returns one whose network is dead, and \code{posterior()}
says so rather than failing later.
}
