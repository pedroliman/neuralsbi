% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnostic_draws}
\alias{diagnostic_draws}
\title{Draw posterior samples for one diagnostic trial, insisting on the full count}
\usage{
diagnostic_draws(post, n, trial)
}
\description{
\code{sample.nsbi_posterior()} returns fewer rows than asked for when a bounded
prior and a leaky estimator leave rejection sampling short, and only warns.
The diagnostics cannot absorb that quietly. \code{sbc()} bins its ranks against
\code{n_posterior_samples}, so a trial that came back short is scored on a scale it
was never drawn on, and the ranks compress toward zero; rescaling that one
trial on its own would instead make it incomparable to the others. Either way
the run reports a miscalibrated posterior when the real cause is lost draws,
so stop and say so.
}
\keyword{internal}
