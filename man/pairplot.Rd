% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{pairplot}
\alias{pairplot}
\title{Visualize posterior samples}
\usage{
pairplot(
  samples,
  truth = NULL,
  labels = NULL,
  limits = NULL,
  col = "steelblue",
  alpha = 0.4,
  ...
)
}
\arguments{
\item{samples}{A matrix of posterior draws (rows = draws), or an
\code{nsbi_samples} object.}

\item{truth}{Optional reference parameter vector to overlay.}

\item{labels}{Optional parameter labels. Defaults to \code{colnames(samples)} (set
automatically for \code{\link[=sample]{sample()}} draws from a fit with named parameters --
see \code{\link[=prior_uniform]{prior_uniform()}}/\code{\link[=prior_normal]{prior_normal()}}) or \code{theta[1]}, \code{theta[2]}, ....
Labels that parse as R syntax (\code{"beta[1]"}, \code{"rho"}) render as their
plotmath symbol.}

\item{limits}{Optional list (one \code{c(lo, hi)} per parameter, in column
order) or matrix of per-parameter axis limits. Defaults to each
parameter's own data range (padded 5\%), applied consistently across every
panel that plots it -- without this, the lower-triangle and diagonal
panels for the same parameter can each draw a different range, since
\code{\link[ggdensity:geom_hdr]{ggdensity::geom_hdr()}} and \code{\link[ggplot2:geom_density]{ggplot2::geom_density()}} estimate their
density grid independently per panel.}

\item{col}{Density-region and marginal-density fill colour.}

\item{alpha}{Marginal-density fill transparency. The lower-triangle
highest-density regions shade themselves by probability level (99/95/80/50\%)
instead, via \code{ggdensity}'s own \code{alpha} mapping.}

\item{...}{Passed to the lower-triangle \code{\link[ggdensity:geom_hdr]{ggdensity::geom_hdr()}} layer.}
}
\value{
A \code{ggmatrix} object (also drawn as a side effect), invisibly.
}
\description{
A pair plot built on \code{\link[GGally:ggpairs]{GGally::ggpairs()}}: 1-D marginal densities on the
diagonal and 2-D highest-density regions (via \code{\link[ggdensity:geom_hdr]{ggdensity::geom_hdr()}}) in
the lower triangle, with optional markers for a reference (e.g. true)
parameter value. Analogous to Python \code{sbi}'s \code{pairplot}.
}
