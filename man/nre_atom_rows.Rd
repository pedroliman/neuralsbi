% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre.R
\name{nre_atom_rows}
\alias{nre_atom_rows}
\title{Which rows of a minibatch each simulation is scored against}
\usage{
nre_atom_rows(b, k, deterministic = FALSE)
}
\description{
Returns a length-\code{b * k} vector of row indices, \code{k} per simulation and
running simulation-major: the true parameter for row \code{i} first, then \code{k - 1}
contrasts drawn without replacement from the \emph{other} rows of the batch. That
is \code{sbi}'s atom construction, done with R's RNG rather than
\code{torch.multinomial} so it is seeded by the same \code{set.seed()} the training
loop's split and batch order already are.
}
\details{
\code{deterministic} freezes the draw. The atoms are resampled every time the
loss is evaluated, which is the objective's business during training but
makes a poor early-stopping signal: the validation loss would move between
epochs because the contrasts changed, not because the classifier did.
Freezing it for the validation pass costs nothing and makes the two numbers
comparable. (\code{sbi} resamples there too, and pays for it in noisier
stopping.)
}
\keyword{internal}
