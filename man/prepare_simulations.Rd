% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npe.R
\name{prepare_simulations}
\alias{prepare_simulations}
\title{Everything both \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}} do before touching a density estimator}
\usage{
prepare_simulations(
  prior,
  simulator,
  n_simulations,
  sim_args,
  theta,
  x,
  standardize,
  seed,
  verbose
)
}
\value{
A list with the cleaned \code{theta}/\code{x}, their standardized versions,
the two \code{nsbi_standardizer}s, the column names, and \code{n_dropped}.
}
\description{
Get the simulations (running the simulator or taking pre-computed ones),
coerce them to matrices, drop non-finite draws, and learn the two
standardizers. Shared so the two entry points cannot drift apart -- they
differ only in which side of \verb{(theta, x)} the estimator treats as its target.
}
\keyword{internal}
