% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulator.R
\name{call_sim_once}
\alias{call_sim_once}
\title{Call the simulator for one parameter set, naming it if it fails}
\usage{
call_sim_once(call_one, theta_i, i = 1L)
}
\arguments{
\item{call_one}{The closure from \code{\link[=simulator_caller]{simulator_caller()}}.}

\item{theta_i}{One parameter set.}

\item{i}{Index of the simulation, for the error message.}
}
\value{
Whatever the simulator returned.
}
\description{
\code{\link[=as_sim_draw]{as_sim_draw()}} reports everything wrong with a simulator's \emph{return value}
and names the simulation it came from. A failure inside the simulator
happens before there is a return value, so on its own it arrives as bare as
R left it: \verb{argument "ls" is missing, with no default}, with no index, no
parameters, and nothing to suggest that the real problem is a dispatch
mismatch rather than a missing default. Under a \pkg{future} plan the call
crosses a worker boundary first, which is where a useful traceback goes.
}
\details{
The original condition is kept as the parent, and its class is carried onto
the re-raised error, so a caller catching a condition the simulator signals
still catches it.
}
\keyword{internal}
