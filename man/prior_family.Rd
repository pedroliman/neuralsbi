% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_families.R
\name{prior_family}
\alias{prior_family}
\title{The distribution families a prior can be built from}
\usage{
prior_family(family)
}
\arguments{
\item{family}{Family name, or anything else to get \code{NULL} back.}
}
\value{
A list, or \code{NULL} for an unregistered name.
}
\description{
One entry per family: the parameter names in Stan's order, the R
density/CDF/quantile trio behind them, and the natural support. Everything
else in this file is generic over that entry, which is why registering a
family is all it takes for \code{\link[=prior_truncated]{prior_truncated()}} to renormalize it and for
\code{\link[=stan_code]{stan_code()}} to write it out.
}
\details{
The \code{d}, \code{p} and \code{q} functions are always called with the value positionally
and the distribution parameters by name, since R spells the first argument
\code{x}, \code{q} and \code{p} in turn while the parameters keep their names throughout.
}
\keyword{internal}
