% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan.R
\name{stan_export}
\alias{stan_export}
\alias{stan_code}
\alias{write_stan_model}
\alias{stan_data}
\title{Export a learned likelihood to Stan}
\usage{
stan_code(fit, name = "nsbi_log_lik", model = TRUE)

write_stan_model(fit, file, name = "nsbi_log_lik", model = TRUE)

stan_data(fit, x_obs = NULL)
}
\arguments{
\item{fit}{An \code{nsbi_nle} object from \code{\link[=nle]{nle()}}.}

\item{name}{Prefix for the generated functions.}

\item{model}{Generate a complete, runnable model (the default) or only the
\code{functions} block, for \verb{#include}-ing into a model of your own.}

\item{file}{Path to write to.}

\item{x_obs}{Observation to put in the data list. Rows are independent
observations.}
}
\value{
\code{stan_code()} returns the Stan program as a single string;
\code{write_stan_model()} returns \code{file} invisibly; \code{stan_data()} returns a
named list ready for \code{cmdstanr} or \code{rstan}.
}
\description{
\code{stan_code()} writes the likelihood learned by \code{\link[=nle]{nle()}} out as Stan source: a
\code{functions} block that recomputes \eqn{\log q_\phi(x \mid \theta)} in Stan's
own language, with the trained weights travelling alongside as data. Stan
differentiates the generated code itself, so NUTS gets exact gradients and
nothing has to be linked against \code{torch} at run time.
}
\section{Why this is useful}{


The exported function is an ordinary Stan function of \code{theta}. That means
the surrogate likelihood stops being the whole model and becomes one term in
a model you write: put a hierarchical prior over the parameters, add
covariates, mix in a second data source with a likelihood you \emph{do} know, and
let NUTS sample the lot. None of that is reachable from a posterior
estimator, which only ever knows the one conditional it was trained on.
}

\section{The generated interface}{


Two entry points, where \code{w} is the packed weight vector from \code{\link[=stan_data]{stan_data()}}:

\if{html}{\out{<div class="sourceCode stan">}}\preformatted{real nsbi_log_lik_lpdf(vector x, vector theta, vector w);      // one observation
real nsbi_log_lik_sum_lpdf(matrix x, vector theta, vector w);  // rows are i.i.d.
}\if{html}{\out{</div>}}

Both take \code{x} and \code{theta} in the original units the simulator and prior use;
the standardization the estimator trained under is folded into the generated
code. Prefer the \verb{_sum} form for repeated observations: for the MDN and the
linear-Gaussian estimator the conditional distribution depends on \code{theta}
alone, so it is computed once and reused across every row.
}

\section{Why the code is generated rather than called}{


Python \code{sbi} hands its learned likelihood to Pyro or PyMC as a callable,
because everything lives in one process and the sampler can differentiate
the same graph the estimator was trained in. Stan cannot work that way: it
compiles to C++ and needs the gradient inside its own autodiff. The
alternative would be an external C++ header (\code{--allow-undefined} plus a
\code{USER_HEADER}) linking \code{torch} into every Stan compile, with the gradients
plumbed by hand. Generating source keeps the run-time dependency at zero and
makes the result something you can read, edit, and check -- which is what the
package's own tests do, by evaluating the emitted functions and comparing
them against \code{\link[=log_lik]{log_lik()}}.
}

\section{Supported estimators}{


\code{"mdn"}, \code{"maf"} and \code{"linear_gaussian"}. \code{"nsf"} is not exported -- its
rational-quadratic spline transform would be a large and fragile block of
generated Stan. Refit with \code{"maf"} if you need a flow.

\code{\link[=nre]{nre()}} fits are not exported at all. What gets transpiled here is a
density; a ratio estimator holds a classifier instead, and there is no
\code{p(x | theta)} in it to write out.
}

\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nle(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 500, density_estimator = "linear_gaussian")

cat(substr(stan_code(fit), 1, 400))
str(stan_data(fit, matrix(rnorm(10), ncol = 1)), max.level = 1)
}
