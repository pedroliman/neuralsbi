% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embedding_mlp}
\alias{embedding_mlp}
\title{Embedding (summary) networks for structured observations}
\usage{
embedding_mlp(output_dim = 16L, hidden = c(64L, 64L))
}
\arguments{
\item{output_dim}{Number of summary features the network emits. This is the
effective data dimension the density estimator conditions on.}

\item{hidden}{Integer vector of hidden-layer widths (ReLU between layers).
An empty vector gives a single linear map to \code{output_dim}.}
}
\value{
An \code{nsbi_embedding} specification. It carries no torch objects (the
network is built lazily at fit time), so it is safe to construct without
\code{torch} installed.
}
\description{
Raw observations are often high-dimensional or structured (a time series, a
set of summary statistics, an image) where feeding \code{x} straight into the
density estimator wastes capacity. An embedding network learns a low-
dimensional summary \eqn{h = f_\psi(x)} jointly with the density estimator,
so the conditioning path becomes \eqn{q_\phi(\theta \mid f_\psi(x))}. This
mirrors \code{sbi}'s \code{embedding_net} argument.
}
\details{
\code{embedding_mlp()} builds a multilayer-perceptron summary network: a stack of
fully connected ReLU layers mapping the (standardized) data to a vector of
\code{output_dim} features. Pass the result to \code{\link[=npe]{npe()}} via \code{embedding_net}; it is
trained end to end with the estimator and its parameters live inside the
fitted network, so sampling and \code{log_prob} route through it automatically.

The embedding consumes the standardized data (the same z-scoring \code{\link[=npe]{npe()}}
applies to \code{x} without an embedding), which keeps the summary network's
inputs on a common scale. Standardization of the \emph{features} is intentionally
left to the network itself; the estimators operate on the raw embedding
output.
}
\examples{
emb <- embedding_mlp(output_dim = 8, hidden = c(64, 64))
# fit <- npe(prior, simulator, density_estimator = "maf", embedding_net = emb)
}
\seealso{
\code{\link[=npe]{npe()}}
}
