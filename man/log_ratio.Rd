% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre.R
\name{log_ratio}
\alias{log_ratio}
\alias{log_ratio.nsbi_nre}
\title{Evaluate a learned likelihood-to-evidence ratio}
\usage{
log_ratio(fit, theta, x, ...)

\method{log_ratio}{nsbi_nre}(fit, theta, x, sum_iid = TRUE, max_batch = 1e+05, ...)
}
\arguments{
\item{fit}{An \code{nsbi_nre} fit from \code{\link[=nre]{nre()}}.}

\item{theta}{Parameter values: a numeric vector (one parameter set) or an
\verb{n_theta x dim_theta} matrix.}

\item{x}{Observed data: a numeric vector (one observation) or an
\verb{n_obs x dim_x} matrix whose rows are independent observations.}

\item{...}{Unused, for S3 consistency.}

\item{sum_iid}{Sum the log ratio over the rows of \code{x} (the default). Set
\code{FALSE} to get the per-observation values instead.}

\item{max_batch}{Largest number of \verb{(theta, x)} pairs evaluated in one call
to the classifier. Only affects memory and speed.}
}
\value{
With \code{sum_iid = TRUE}, a numeric vector with one entry per row of
\code{theta}. With \code{sum_iid = FALSE}, an \verb{n_theta x n_obs} matrix.
}
\description{
\code{log_ratio()} evaluates the ratio learned by \code{\link[=nre]{nre()}},
\eqn{\log r(\theta, x) = \log p(x \mid \theta) - \log p(x)}.
}
\details{
Rows of \code{x} are treated as \strong{independent observations from the same
parameter}, so by default the result sums over them. That sum is the
log-likelihood of the whole data set up to an additive constant that does
not depend on \code{theta}, which is all a posterior needs and all NRE learns:
the evidence term \eqn{\log p(x)} is never estimated separately, so
\code{log_ratio()} is not a log-likelihood you can compare across observations.
Differences between two \code{theta} at the same \code{x} are exactly the differences
in log-likelihood.
}
\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nre(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 1000, classifier = "logistic")

x_obs <- matrix(rnorm(20, mean = 1, sd = 0.5), ncol = 1)
grid <- matrix(seq(-2, 2, length.out = 5), ncol = 1)
log_ratio(fit, grid, x_obs)
}
\seealso{
\code{\link[=posterior]{posterior()}} to turn the ratio into posterior draws, \code{\link[=log_lik]{log_lik()}}
for the \code{\link[=nle]{nle()}} equivalent that \emph{is} a calibrated log-likelihood.
}
