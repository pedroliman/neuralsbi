% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nre_posterior.R
\name{posterior.nsbi_nre}
\alias{posterior.nsbi_nre}
\title{Posterior from a neural ratio}
\usage{
\method{posterior}{nsbi_nre}(
  fit,
  x_obs = NULL,
  n_chains = 20L,
  warmup = 200L,
  thin = 2L,
  init_strategy = c("resample", "proposal"),
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{An \code{nsbi_nre} object from \code{\link[=nre]{nre()}}.}

\item{x_obs}{Observation to condition on. Rows are treated as independent
observations from the same parameter, and the log ratio sums over them.}

\item{n_chains}{Number of chains. The slice sampler evaluates every chain in
one batched call per step, so more chains cost almost nothing.}

\item{warmup}{Steps discarded at the start of each chain.}

\item{thin}{Keep one draw in \code{thin}. See \code{\link[=posterior.nsbi_nle]{posterior.nsbi_nle()}} for why the
default is 2.}

\item{init_strategy}{\code{"resample"} (default, and \code{sbi}'s) weights a pool of
prior draws by the posterior density and resamples the starting points
from it; \code{"proposal"} starts from plain prior draws.}

\item{seed}{Optional integer seed.}

\item{...}{Further arguments to the sampler: \code{width}, \code{max_steps} and
\code{n_pool}.}
}
\value{
An object of class
\code{c("nsbi_nre_posterior", "nsbi_mcmc_posterior", "nsbi_posterior")}.
}
\description{
Bayes' rule turns an \code{\link[=nre]{nre()}} fit into a posterior,
\eqn{p(\theta \mid x) \propto r_\phi(\theta, x)\,p(\theta)}, and as with
\code{\link[=nle]{nle()}} the result has no closed form and no direct sampler. \code{posterior()}
on an \code{nsbi_nre} therefore returns an object that samples with MCMC, and
\code{\link[=sample]{sample()}} on it runs a chain rather than a forward pass.
}
\details{
There is one sampler here, the vectorized slice sampler of \link{nsbi_mcmc}, and
no \code{sampler} argument to choose it with. \code{\link[=nle]{nle()}}'s \code{"stan"} option works by
transpiling the fitted density into a Stan \code{functions} block; the residual
classifier behind a ratio estimator has no such export, so NUTS is not on
offer.
}
\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nre(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 1000, classifier = "logistic")

x_obs <- matrix(rnorm(50, mean = 1, sd = 0.5), ncol = 1)
post <- posterior(fit, x_obs, n_chains = 4, warmup = 50, thin = 2)
draws <- sample(post, 400)
colMeans(draws)
}
