% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nle_posterior.R
\name{posterior.nsbi_nle}
\alias{posterior.nsbi_nle}
\title{Posterior from a neural likelihood}
\usage{
\method{posterior}{nsbi_nle}(
  fit,
  x_obs = NULL,
  sampler = c("slice", "stan"),
  n_chains = NULL,
  warmup = 200L,
  thin = 2L,
  init_strategy = c("resample", "proposal"),
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{An \code{nsbi_nle} object from \code{\link[=nle]{nle()}}.}

\item{x_obs}{Observation to condition on. Rows are treated as independent
observations from the same parameter, and the log-likelihood sums over
them.}

\item{sampler}{\code{"slice"} (the default) or \code{"stan"}.}

\item{n_chains}{Number of chains. The default depends on the sampler: 20 for
\code{"slice"}, which evaluates every chain in one batched call per step and so
pays almost nothing for more of them, and 4 for \code{"stan"}, where each chain
is a separate process with its own warmup to pay for.}

\item{warmup}{Steps discarded at the start of each chain.}

\item{thin}{Keep one draw in \code{thin}. The default is 2: the slice width is
adapted during warmup, and with that the retained draws are already close
to independent -- on a Gaussian target with 20 chains, \code{thin = 2} gives a
bulk ESS of about 96\% of them. Since each evaluation is a forward pass over
every observation, thinning harder buys very little for what it costs.
Raise it if the reported ESS says you need to. (Python \code{sbi} thins by 1 by
default; it thinned by 10 up to v0.21.)}

\item{init_strategy}{\code{"resample"} (default, and \code{sbi}'s) weights a pool of
prior draws by the posterior density and resamples the starting points
from it, drawing more pools as needed if the first one lands fewer than
\code{n_chains} draws in the posterior's support; \code{"proposal"} skips the
weighting and keeps whichever prior draws land inside the posterior's
support, also drawing more pools as needed. \code{"proposal"} is cheaper per
accepted draw, but every draw the posterior excludes is wasted: if only a
fraction \code{a} of the prior's mass survives, roughly \code{1 / a} draws are
needed per starting point, so a posterior that rules out most of the
prior can make \code{"proposal"} far slower than \code{"resample"} to find enough
starting points, and it errors out once its draw budget is spent. The
pool is 1000 draws, set with \code{n_pool}. \code{sbi} uses 10,000 \emph{per chain},
which on a surrogate summed over thousands of observations is a large
bill before the first MCMC step.}

\item{seed}{Optional integer seed.}

\item{...}{Further arguments to the sampler: \code{width}, \code{max_steps} and
\code{n_pool} for \code{"slice"}, or \code{iter_warmup}, \code{iter_sampling} and \code{refresh}
for \code{"stan"}.}
}
\value{
An object of class
\code{c("nsbi_nle_posterior", "nsbi_mcmc_posterior", "nsbi_posterior")}.
}
\description{
Bayes' rule turns an \code{\link[=nle]{nle()}} fit into a posterior,
\eqn{p(\theta \mid x) \propto q_\phi(x \mid \theta)\,p(\theta)}, but the
result has no closed form and no direct sampler. \code{posterior()} on an
\code{nsbi_nle} therefore returns an object that samples with MCMC, and
\code{\link[=sample]{sample()}} on it runs a chain rather than a forward pass.
}
\details{
Two samplers are available. \code{"slice"} is the default: a vectorized
univariate slice sampler (see \link{nsbi_mcmc}) with nothing to tune and no
dependencies. \code{"stan"} writes the fitted likelihood out as a Stan program
(see \code{\link[=stan_code]{stan_code()}}) and runs NUTS on it through \pkg{cmdstanr} or
\pkg{rstan}, which mixes better on correlated posteriors at the cost of a
one-time model compile.
}
\examples{
prior <- prior_uniform(c(mu = -3), c(mu = 3))
fit <- nle(prior, function(mu) c(y = rnorm(1, mu, 0.5)),
           n_simulations = 1000, density_estimator = "linear_gaussian")

x_obs <- matrix(rnorm(50, mean = 1, sd = 0.5), ncol = 1)
post <- posterior(fit, x_obs, n_chains = 4, warmup = 50, thin = 2)
draws <- sample(post, 400)
colMeans(draws)
}
