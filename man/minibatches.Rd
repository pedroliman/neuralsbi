% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{minibatches}
\alias{minibatches}
\title{Split a shuffled row order into minibatches}
\usage{
minibatches(order, batch_size)
}
\arguments{
\item{order}{Row indices, already shuffled.}

\item{batch_size}{Rows per batch.}
}
\value{
A list of index vectors covering \code{order} in order.
}
\description{
Stepping through the rows by \code{batch_size} leaves a short final batch, and a
final batch of \emph{one} row is worse than short. \code{\link[=nre]{nre()}}'s atomic loss has no
contrast to score a single simulation against, so it returns a constant that
torch did not build and \code{backward()} errors with "element 0 of tensors does
not require grad"; every other estimator takes a gradient step from one
sample. Those rows join the previous batch instead. It costs one batch of
\code{batch_size + 1} once an epoch and removes a failure that depends on nothing
but \code{n_simulations} modulo \code{batch_size}.
}
\keyword{internal}
