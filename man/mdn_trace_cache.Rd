% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{mdn_trace_cache}
\alias{mdn_trace_cache}
\title{Replay the MDN's i.i.d. density as TorchScript instead of driving it from R}
\usage{
mdn_trace_cache(de, xt, max_batch, eager, warmup = 4L)
}
\arguments{
\item{de, xt, max_batch}{As in \code{\link[=mdn_iid_blocks]{mdn_iid_blocks()}}, with the observations
already a tensor.}

\item{eager}{The evaluator to check each trace against, and to fall back to.}

\item{warmup}{Calls to serve eagerly before recording anything.}
}
\value{
\verb{function(theta)} returning a traced function for that many rows, or
\code{NULL} when the eager path should be used. \code{NULL} if tracing is switched
off.
}
\description{
Every operation in \code{\link[=mdn_iid_blocks]{mdn_iid_blocks()}} crosses from R into libtorch, and at
MCMC batch sizes that crossing costs far more than the arithmetic behind it:
about 0.2 ms each, thirty of them per evaluation, against a few hundred
microseconds of actual work. \code{\link[torch:jit_trace]{torch::jit_trace()}} records the same
computation once and replays it inside libtorch, so an evaluation costs one
crossing rather than thirty.
}
\details{
It is the same code either way. Tracing runs the eager path and records what
it did, so there is no second implementation of the density to keep in step
with this one or with the Stan generator.

Three things make this a shortcut rather than the path. Recording a trace
costs several evaluations' worth of time, so nothing is recorded until the
evaluator has been called \code{warmup} times and it is clear this is a loop
rather than a one-off; a single \code{\link[=log_lik]{log_lik()}} call should not pay for a
compiler. A trace fixes the shapes it was recorded at, so there is one per
number of parameter rows, checked against the eager result before anything
uses it. And it is only worth recording when the whole observation set fits
in one chunk, since otherwise the graph unrolls the chunk loop. Failing any
of these is not an error: the caller falls back to the eager path, which is
why \code{NULL} is a perfectly good answer here.

Set \code{options(neuralsbi.jit = FALSE)} to skip tracing entirely.
}
\keyword{internal}
