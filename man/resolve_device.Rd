% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_device}
\alias{resolve_device}
\title{Resolve a validated \code{device} keyword to a concrete, available torch device}
\usage{
resolve_device(device)
}
\arguments{
\item{device}{One of \code{"cpu"}, \code{"cuda"}, \code{"mps"}, \code{"gpu"}, \code{"auto"}.}
}
\value{
\code{"cpu"}, \code{"cuda"} or \code{"mps"}.
}
\description{
\code{\link[=check_device_arg]{check_device_arg()}} has already rejected anything but \code{"cpu"}, \code{"cuda"},
\code{"mps"}, \code{"gpu"} or \code{"auto"}; this turns that keyword into \code{"cpu"},
\code{"cuda"} or \code{"mps"}, matching what Python \code{sbi}'s \code{process_device()} does.
Requires \code{torch} (call \code{\link[=require_torch]{require_torch()}} first; every caller here is about
to build a network, so it already has).
}
\details{
\code{"cuda"}/\code{"mps"} name a specific device, so asking for one that is not
there errors rather than downgrading silently -- a silent fallback would
hide a real problem (a missing CUDA build, a non-Apple-silicon Mac).
\code{"gpu"}/\code{"auto"} never named a specific device, so it resolves
CUDA -> MPS -> CPU and \emph{can} fall back silently, mirroring \code{sbi}'s \code{"gpu"}.
}
\keyword{internal}
