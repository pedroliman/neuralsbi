% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cat_fit_common}
\alias{cat_fit_common}
\title{Print the fit-summary block shared by \code{print.nsbi_npe()}, \code{print.nsbi_nle()}
and \code{print.nsbi_snpe()}}
\usage{
cat_fit_common(x, save_fn_name, data_suffix = "")
}
\arguments{
\item{x}{An \code{nsbi_npe}- or \code{nsbi_nle}-family fit.}

\item{save_fn_name}{Name of the save function to point to in the
dead-network warning, e.g. \code{"save_npe"}; the matching \verb{load_*()} name is
derived from it.}

\item{data_suffix}{Text appended to the "data (dim)" line before its
newline, e.g. \code{"  per observation"} for \code{\link[=nle]{nle()}}.}
}
\description{
Parameter names, outcome names, the embedding line (when the fit used
one), the simulation count and any drops, the best validation loss, and
the dead-network warning read the same fields regardless of which
factorization was learned. \code{save_fn_name} is the one line that legitimately
differs between an NPE and an NLE fit, so it is the one argument callers
must supply.
}
\keyword{internal}
