% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npe.R
\name{npe}
\alias{npe}
\title{Neural Posterior Estimation (NPE)}
\usage{
npe(
  prior,
  simulator = NULL,
  n_simulations = 1000,
  theta = NULL,
  x = NULL,
  density_estimator = c("mdn", "linear_gaussian"),
  n_components = 5L,
  hidden = c(50L, 50L),
  max_epochs = 500L,
  batch_size = 100L,
  lr = 5e-04,
  validation_fraction = 0.1,
  patience = 20L,
  standardize = TRUE,
  seed = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{prior}{An \code{nsbi_prior} (see \code{\link[=prior_uniform]{prior_uniform()}}, \code{\link[=prior_normal]{prior_normal()}}).}

\item{simulator}{A function mapping an \verb{n x dim} matrix of parameters to an
\verb{n x d} matrix of simulated data. Ignored if \code{theta} and \code{x} are given.}

\item{n_simulations}{Number of prior draws to simulate when \code{simulator} is
used and \code{theta}/\code{x} are not supplied.}

\item{theta, x}{Optional pre-computed simulations. If supplied, \code{simulator}
and \code{n_simulations} are ignored.}

\item{density_estimator}{Either \code{"mdn"} (neural Mixture Density Network,
needs \code{torch}) or \code{"linear_gaussian"} (closed-form baseline, no \code{torch}),
or a function \verb{function(theta, x)} returning a fitted estimator.}

\item{n_components, hidden}{MDN settings: number of mixture components and a
vector of hidden-layer widths.}

\item{max_epochs, batch_size, lr, validation_fraction, patience}{MDN training
controls (Adam optimizer, early stopping on validation loss).}

\item{standardize}{Whether to z-score \code{theta} and \code{x} before training
(strongly recommended; default \code{TRUE}).}

\item{seed}{Optional integer seed for reproducibility.}

\item{verbose}{Print training progress.}

\item{...}{Passed to the density estimator.}
}
\value{
An object of class \code{nsbi_npe}. Turn it into a usable posterior with
\code{\link[=posterior]{posterior()}}, or sample directly with \code{\link[=sample]{sample()}}.
}
\description{
\code{npe()} is the main entry point. Given a prior and either a simulator (which
it will call) or a set of pre-computed simulations \verb{(theta, x)}, it trains a
conditional density estimator whose output directly approximates the posterior
\eqn{p(\theta \mid x)}. This is single-round, \emph{amortized} NPE: after training
once, you can condition on any observation without re-simulating.
}
\examples{
\donttest{
prior <- prior_uniform(c(-2, -2, -2), c(2, 2, 2))
simulator <- function(theta) theta + 1 + matrix(rnorm(length(theta), sd = 0.1),
                                                 nrow = nrow(theta))
fit <- npe(prior, simulator, n_simulations = 2000)
post <- posterior(fit, x_obs = c(0.8, 0.6, 0.4))
draws <- sample(post, 1000)
}
}
