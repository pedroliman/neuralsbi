% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{de_log_lik_iid}
\alias{de_log_lik_iid}
\title{Log-density of many observations under many parameter values}
\usage{
de_log_lik_iid(de, x, theta, max_batch = 1e+05)
}
\arguments{
\item{de}{A fitted density estimator.}

\item{x}{Standardized observations, \verb{n_obs x dim_x}.}

\item{theta}{Standardized parameters, \verb{n_theta x dim_theta}.}

\item{max_batch}{Largest number of pairs evaluated at once.}
}
\value{
An \verb{n_theta x n_obs} matrix of log-densities.
}
\description{
The cross product \verb{n_theta x n_obs}, in standardized space. Only
\code{sum_iid = FALSE} wants that matrix; everything else -- \code{\link[=log_lik]{log_lik()}}'s default
and every MCMC step -- wants its row sums, and gets them from
\code{\link[=de_iid_evaluator]{de_iid_evaluator()}} without ever building it.
}
\details{
The default expands the cross product and makes one batched call, which is
what a flow needs: its transforms depend on the observation as well as the
parameter, so there is nothing to reuse between observations. Estimators
whose conditional distribution depends on the parameter \emph{alone} -- the MDN
and the linear-Gaussian baseline -- override this and compute that
distribution once per parameter, which turns the i.i.d. sum from
\code{n_theta * n_obs} network passes into \code{n_theta} of them. With a few thousand
observations that is the difference between usable and not.
}
\keyword{internal}
