% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{surrogate_potential}
\alias{surrogate_potential}
\title{Unnormalized log posterior of a surrogate fit}
\usage{
surrogate_potential(fit, x_obs, max_batch = 1e+05)
}
\arguments{
\item{fit}{An \code{nsbi_nle} or \code{nsbi_nre} fit.}

\item{x_obs}{The observation to condition on. Rows are independent
observations of the same parameter.}

\item{max_batch}{Largest number of \verb{(theta, x)} pairs evaluated at once. No
caller overrides it today, but it is a real tuning knob -- the batch size
the MCMC evaluations are chunked into -- that a future caller would
plausibly want to change, so it stays a parameter.}
}
\value{
\verb{function(theta)} giving one unnormalized log posterior density per
row.
}
\description{
\eqn{\log q_\phi(x \mid \theta) + \log p(\theta)} for an \code{\link[=nle]{nle()}} fit and
\eqn{\log r_\phi(\theta, x) + \log p(\theta)} for an \code{\link[=nre]{nre()}} fit, returning
\code{-Inf} outside the prior support. This is the potential the MCMC samplers
target, and the two fits differ only in which surrogate contributes the
first term.
}
\keyword{internal}
