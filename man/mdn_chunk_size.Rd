% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{mdn_chunk_size}
\alias{mdn_chunk_size}
\title{How many observations to score in one call}
\usage{
mdn_chunk_size(n_theta, max_batch, per_pair)
}
\description{
\code{max_batch} counts (theta, x) pairs, so that is what sizes the chunk. The
quadratic form materializes \code{K} components and \code{p} dimensions per pair on top
of that, so a second cap keeps the \verb{(T, K, n, p)} intermediate to a few tens
of megabytes however many components the mixture has. Dividing by \code{K * p}
alone, as this used to, chunked a 5000-observation call ten ways and paid the
fixed cost of a torch call ten times for a 400 KB intermediate.
}
\keyword{internal}
