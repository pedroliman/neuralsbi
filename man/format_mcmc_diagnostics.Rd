% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{format_mcmc_diagnostics}
\alias{format_mcmc_diagnostics}
\title{Summarize MCMC diagnostics for printing}
\usage{
format_mcmc_diagnostics(d)
}
\arguments{
\item{d}{The data frame from \code{\link[=mcmc_diagnostics]{mcmc_diagnostics()}}.}
}
\value{
One line of text, without a trailing newline.
}
\description{
\code{split_rhat()} and \code{bulk_ess()} return \code{NA} for a run they cannot score: too
few iterations, one chain, or a coordinate that never moved. Every parameter
can come back \code{NA} at once, and \code{max(na.rm = TRUE)} over all-\code{NA} is \code{-Inf}
with a warning, which the print method used to show as if it were a value.
A partially scored run is reported from the parameters that did get a
number, with a count of the ones that did not.
}
\keyword{internal}
