% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_families.R
\name{prior_independent}
\alias{prior_independent}
\title{Combine independent priors into one joint prior}
\usage{
prior_independent(...)
}
\arguments{
\item{...}{Priors, one per block of parameters. Name them
(\code{prior_independent(beta = ..., gamma = ...)}) to name the parameters: a
name given here beats the component's own \code{param_names} for a one-parameter
component, and a multi-parameter component keeps its own names, or takes
\code{name1}, \code{name2}, ... when it has none.}
}
\value{
An \code{nsbi_prior} object.
}
\description{
Stan writes a joint prior as one sampling statement per parameter and lets
the product take care of itself. This is that, as an object: give it the
per-parameter priors in the order the simulator expects them and it returns
the product prior, with \code{dim} the total, \code{lower}/\code{upper} stacked, and a
\code{log_prob} that sums the parts. It is the thing most \code{\link[=prior_custom]{prior_custom()}} calls
were written to do by hand.
}
\details{
Components may themselves cover several parameters, so
\code{prior_independent(prior_normal(mean = c(0, 0)), prior_beta(2, 15))} is a
three-parameter prior. Any \code{nsbi_prior} works as a component, including a
\code{\link[=prior_custom]{prior_custom()}}; the result can only be written out by \code{\link[=stan_code]{stan_code()}} when
every component comes from a named family (see \link{prior_families}).
}
\examples{
prior <- prior_independent(
  p_S1S2 = prior_beta(2, 15),
  hr_S1  = prior_lognormal(log(3), 0.3),
  hr_S2  = prior_lognormal(log(10), 0.25)
)
prior
sample_prior(prior, 3)
}
\seealso{
\link{prior_families} for the components, \code{\link[=prior_truncated]{prior_truncated()}} to bound
one, \link{priors} for the rest.
}
