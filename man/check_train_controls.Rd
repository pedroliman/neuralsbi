% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{check_train_controls}
\alias{check_train_controls}
\title{Validate the training controls}
\usage{
check_train_controls(
  max_epochs,
  batch_size,
  lr,
  validation_fraction,
  patience,
  n_restarts,
  clip_grad_norm,
  n = NULL
)
}
\arguments{
\item{max_epochs, batch_size, lr, validation_fraction, patience}{Neural training
controls (Adam optimizer, early stopping on validation loss). The defaults
(\code{batch_size = 200}, \code{lr = 5e-4}, \code{validation_fraction = 0.1},
\code{patience = 20}) match Python \code{sbi}; \code{max_epochs} is a high guard cap that
early stopping normally reaches first.}

\item{n_restarts}{Train this many independently initialized networks and keep
the one with the best validation loss (guards against bad initializations
and MDN mode collapse).}

\item{clip_grad_norm}{Maximum gradient norm during training (\code{Inf} disables
clipping). The learning rate also decays 2x after 10 epochs without
validation improvement.}

\item{n}{Number of training rows, or \code{NULL} when they do not exist yet.}
}
\description{
Every one of these decides how the split, the batches or the restart loop
is built, and an unchecked value is reported by whichever base function
hits it first: \code{batch_size = 0} comes back as "invalid '(to - from)/by'",
\code{validation_fraction = 1} as "wrong sign in 'by' argument", and
\code{n_restarts = 0} as "Training failed: no restart produced a finite
validation loss", which blames training for an argument. \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}}
call this before they simulate, so a typo does not cost the budget first.
}
\details{
\code{n} is optional because that call happens before there are any rows. When it
is known, \code{validation_fraction} is checked against it: the requirement is
that both sides of the split come out non-empty, which the fraction alone
cannot decide.
}
\keyword{internal}
