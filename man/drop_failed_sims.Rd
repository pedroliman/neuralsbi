% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulator.R
\name{drop_failed_sims}
\alias{drop_failed_sims}
\title{Drop simulations whose parameters or output are not finite}
\usage{
drop_failed_sims(theta, x, what = "simulations")
}
\arguments{
\item{theta}{Parameter matrix, or \code{NULL} when the caller has no parameters
to keep aligned.}

\item{x}{Simulated data matrix.}

\item{what}{Noun for the warning ("simulations", "SBC trials", ...).}
}
\description{
A row survives only when every value in both \code{theta} and \code{x} is finite, and
the two are subset together so the training set stays paired. Checking
\code{theta} matters on the pre-computed path, where a user-supplied \code{NA} would
otherwise reach \code{chol()} in the estimator and surface as a linear-algebra
error. Returns the surviving pair and the number dropped; warns once, and
errors when nothing survives. See the "Failed simulations" section of
\link{nsbi_simulator}.
}
\keyword{internal}
