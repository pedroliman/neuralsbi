% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_families.R
\name{prior_families}
\alias{prior_families}
\alias{prior_lognormal}
\alias{prior_exponential}
\alias{prior_gamma}
\alias{prior_beta}
\alias{prior_student_t}
\alias{prior_cauchy}
\alias{prior_half_normal}
\alias{prior_half_cauchy}
\title{Named prior families}
\usage{
prior_lognormal(meanlog = 0, sdlog = 1)

prior_exponential(rate = 1)

prior_gamma(shape, rate = 1)

prior_beta(shape1, shape2)

prior_student_t(df, location = 0, scale = 1)

prior_cauchy(location = 0, scale = 1)

prior_half_normal(sd = 1)

prior_half_cauchy(scale = 1)
}
\arguments{
\item{meanlog, sdlog}{Log-scale mean and standard deviation of the
log-normal, as in \code{\link[stats:dlnorm]{stats::dlnorm()}} and Stan's \code{lognormal}.}

\item{rate}{Rate of the exponential or gamma, as in \code{\link[stats:dexp]{stats::dexp()}} and
Stan's \code{exponential}/\code{gamma}. Must be positive.}

\item{shape}{Shape of the gamma. Must be positive.}

\item{shape1, shape2}{Beta shape parameters, as in \code{\link[stats:dbeta]{stats::dbeta()}}. Both
must be positive.}

\item{df}{Degrees of freedom of the Student-t. Must be positive.}

\item{location, scale}{Location and scale of the Student-t, the Cauchy or the
half-Cauchy. \code{scale} must be positive.}

\item{sd}{Standard deviation of the normal underlying the half-normal. Must
be positive.}
}
\value{
An \code{nsbi_prior} object.
}
\description{
Priors built from a named distribution family, one marginal per parameter,
following Stan's argument names and argument order. Each constructor is
vectorized: pass a vector where Stan would write one sampling statement per
component, and pass a scalar to use the same value for every parameter.
Naming any of the vectors (e.g. \code{c(beta = 2, gamma = 3)}) names the
parameters, exactly as naming \code{low} does in \code{\link[=prior_uniform]{prior_uniform()}}.
}
\details{
Families with constrained support set \code{lower}/\code{upper} on the prior, so the
posterior's leakage correction (see \code{\link[=posterior]{posterior()}}) rejects and renormalizes
against the right region without any further declaration. A gamma prior
bounds at zero, a beta prior at zero and one, and the half families at zero.

A prior built here can be truncated further with \code{\link[=prior_truncated]{prior_truncated()}},
combined with others through \code{\link[=prior_independent]{prior_independent()}}, and written out by
\code{\link[=stan_code]{stan_code()}} as the sampling statement it came from. That is the reason
these exist as families rather than as recipes for \code{\link[=prior_custom]{prior_custom()}}: the
family is what carries over to Stan and to the truncation constant.

\code{prior_half_normal()} and \code{prior_half_cauchy()} are the zero-centred
families truncated to \eqn{[0, \infty)}, matching Stan's idiom of declaring
\verb{real<lower=0>} and writing \code{sigma ~ normal(0, 1)}. Their densities carry
the \code{log(2)} renormalization, which Stan drops as a constant and this
package cannot. For a half-normal centred somewhere other than zero, wrap
\code{\link[=prior_normal]{prior_normal()}} in \code{\link[=prior_truncated]{prior_truncated()}}.
}
\examples{
# One log-normal per parameter, named through the vector.
prior <- prior_lognormal(meanlog = c(beta = log(0.4), gamma = log(0.125)),
                         sdlog = c(0.5, 0.2))
theta <- sample_prior(prior, 5)

# A scalar argument is shared: three gammas with the same rate.
prior_gamma(shape = c(2, 5, 9), rate = 3)

# Support bounds come from the family, and drive the leakage correction.
within_support(prior_beta(2, 15), c(0.1, 1.5))
}
\seealso{
\code{\link[=prior_independent]{prior_independent()}} to combine several of these into one joint
prior, \code{\link[=prior_truncated]{prior_truncated()}} to bound one, and \link{priors} for the rest.
}
