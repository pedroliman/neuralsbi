% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nle_posterior.R
\name{sample.nsbi_nle_posterior}
\alias{sample.nsbi_nle_posterior}
\title{Sample an NLE posterior with MCMC}
\usage{
\method{sample}{nsbi_nle_posterior}(
  x,
  size = 1000,
  n = size,
  obs = NULL,
  refresh = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{An \code{nsbi_nle_posterior} object (named \code{x} to satisfy the \code{\link[=sample]{sample()}}
generic).}

\item{size, n}{Number of posterior draws (\code{n} is an alias for \code{size}).}

\item{obs}{Observation to condition on (defaults to the posterior's \code{x_obs}).}

\item{refresh}{Force a new run even when a cached one would do.}

\item{verbose}{Report sampling progress.}

\item{...}{Unused.}
}
\value{
An \verb{n x dim} matrix of posterior draws (class \code{nsbi_samples}), with
convergence diagnostics attached as attribute \code{diagnostics}.
}
\description{
Runs the sampler chosen by \code{\link[=posterior.nsbi_nle]{posterior.nsbi_nle()}}. Draws are cached on the
posterior object, so asking for the same or fewer draws from the same
observation returns immediately instead of re-running a chain -- which is
what makes \code{\link[=summary]{summary()}} and repeated calls tolerable.
}
