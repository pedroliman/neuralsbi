% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{de_iid_evaluator}
\alias{de_iid_evaluator}
\title{A summed i.i.d. log-likelihood with the observation held fixed}
\usage{
de_iid_evaluator(de, x, max_batch = 1e+05)
}
\arguments{
\item{de}{A fitted density estimator.}

\item{x}{Standardized observations, \verb{n_obs x dim_x}.}

\item{max_batch}{Largest number of pairs evaluated at once.}
}
\value{
\verb{function(theta)} giving one summed log-density per row of \code{theta},
in standardized space.
}
\description{
\code{\link[=log_lik]{log_lik()}} and every MCMC step ask the same question over and over: the
summed log-density of one fixed set of observations under a \code{theta} that
changes. \code{de_iid_evaluator()} returns a closure over the observations, so
whatever an estimator can settle once settles when the closure is built
rather than on every call. For the MDN that is coercing the observations to
a tensor, which at a few thousand rows is not a rounding error next to the
forward pass.
}
\details{
Reducing inside the closure matters as much as the hoisting. The
\verb{n_theta x n_obs} matrix is the largest object in the loop and none of it is
wanted, so the sum happens where the log-densities are produced and only
\code{n_theta} numbers ever cross back into R.

Estimators need a method here only if they can beat the default, which is
\code{\link[=de_log_lik_iid]{de_log_lik_iid()}} with its row sums taken block by block.
}
\keyword{internal}
