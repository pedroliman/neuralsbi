% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_device_arg}
\alias{check_device_arg}
\title{Validate the \code{device} argument shared by \code{\link[=npe]{npe()}} and \code{\link[=nle]{nle()}}}
\usage{
check_device_arg(device)
}
\arguments{
\item{device}{The user's value.}
}
\value{
\code{device}, unchanged.
}
\description{
Only checks that \code{device} is one of the recognized keywords -- \code{"cpu"},
\code{"cuda"}, \code{"mps"}, or \code{"gpu"}/\code{"auto"} -- so a typo is caught before the
simulator runs, the same reason \code{\link[=check_train_controls]{check_train_controls()}} runs early. This
is the whole check for \code{"cpu"}. Turning \code{"cuda"}/\code{"mps"}/\code{"gpu"}/\code{"auto"}
into a concrete, available device needs \code{torch} loaded, which
\code{density_estimator = "linear_gaussian"} never requires; that step is
\code{\link[=resolve_device]{resolve_device()}}'s job, called only once a \code{torch}-backed estimator is
about to train.
}
\keyword{internal}
