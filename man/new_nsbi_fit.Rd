% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npe.R
\name{new_nsbi_fit}
\alias{new_nsbi_fit}
\title{Assemble the object an \code{\link[=npe]{npe()}}, \code{\link[=nle]{nle()}} or \code{\link[=nre]{nre()}} call returns}
\usage{
new_nsbi_fit(de, prior, prep, class, extra = list())
}
\arguments{
\item{de}{The fitted estimator. A ratio estimator travels in the same slot
as a density estimator, since nothing downstream cares which it is until
it comes time to score a \verb{(theta, x)} pair.}

\item{prior}{The prior the simulations were drawn from.}

\item{prep}{The list from \code{\link[=prepare_simulations]{prepare_simulations()}}.}

\item{class}{The class to stamp on the result.}

\item{extra}{Named list of fields specific to this fit type, inserted before
\code{device}.}
}
\description{
The three differ in the estimator they fit, the class they carry and a field
or two naming the architecture. Everything else -- the fitted estimator, the
prior, the two standardizers, the dimensions, the names, the simulation
counts, the device -- is the same list in the same order, and every consumer
downstream (\code{save_npe()}, \code{check_fit_alive()}, \code{cat_fit_common()},
\code{summary()}) reads it by name from all three.
}
\keyword{internal}
