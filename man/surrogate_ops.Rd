% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{surrogate_ops}
\alias{surrogate_ops}
\title{How a surrogate fit is scored}
\usage{
surrogate_ops(fit)
}
\arguments{
\item{fit}{An \code{nsbi_nle} or \code{nsbi_nre} fit.}
}
\value{
A list with \code{matrix_fn}, \code{evaluator} and \code{log_jac}.
}
\description{
\code{\link[=nle]{nle()}} and \code{\link[=nre]{nre()}} fits are interchangeable everywhere downstream of the
estimator, and they differ in exactly three things: which function produces
the \verb{n_theta x n_obs} matrix of scores, which one produces its row sums
without building it, and whether standardizing \code{x} needs a
change-of-variables term. A density reported in the simulator's units needs
one; a ratio does not, since the Jacobian cancels between its numerator and
denominator (see \code{\link[=nre]{nre()}}).
}
\details{
Keeping the three together in one table is what lets \code{\link[=surrogate_score]{surrogate_score()}} and
\code{\link[=surrogate_potential]{surrogate_potential()}} be plain functions rather than a generic each. It
also means the fact that a ratio has no Jacobian is written down once.
}
\keyword{internal}
