% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_family_param}
\alias{check_family_param}
\title{Validate one distribution parameter of a named prior family}
\usage{
check_family_param(value, arg, positive = FALSE)
}
\arguments{
\item{value}{The user's value.}

\item{arg}{Name of the argument.}

\item{positive}{Require every entry to be strictly positive, for a scale, a
rate or a shape.}
}
\value{
\code{value}, unchanged.
}
\description{
The constructors in \link{prior_families} take numeric vectors, one entry per
parameter or one shared entry, and every one of them ends up inside a
\verb{stats::d*()}/\verb{q*()} pair. Those are quiet about nonsense: \code{dgamma(x, shape = -1)} returns \code{NaN} with a warning, \code{qgamma(u, shape = 0)} returns zeros,
and either way the mistake surfaces as a prior sample full of \code{NaN}s once
the simulation budget is already spent. The values are listed rather than
described, as \code{\link[=check_counts]{check_counts()}} does, since which entry is wrong is the thing
worth reading.
}
\details{
Names are left on the value: \code{\link[=family_param_names]{family_param_names()}} reads the parameter
names off whichever argument carries them, and that has to happen after
validation.
}
\keyword{internal}
