% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{make_potential}
\alias{make_potential}
\title{The body every \code{surrogate_potential()} method shares}
\usage{
make_potential(fit, x_obs, evaluator, log_jac, max_batch = 1e+05)
}
\arguments{
\item{fit}{An \code{nsbi_nle} or \code{nsbi_nre} fit.}

\item{x_obs}{The observation to condition on. Rows are independent
observations of the same parameter.}

\item{evaluator}{\verb{function(de, x_z, max_batch)} returning the summed
per-observation score as a function of standardized parameters.}

\item{log_jac}{Apply the \code{x} standardizer's log-Jacobian, once per
observation.}

\item{max_batch}{Largest number of \verb{(theta, x)} pairs evaluated at once. No
caller overrides it today, but it is a real tuning knob -- the batch size
the MCMC evaluations are chunked into -- that a future caller would
plausibly want to change, so it stays a parameter.}
}
\description{
\code{evaluator} is what turns the fitted estimator and the observation into a
\verb{function(theta)}: \code{\link[=de_iid_evaluator]{de_iid_evaluator()}} for a learned density,
\code{\link[=nre_iid_evaluator]{nre_iid_evaluator()}} for a learned ratio. \code{log_jac} says whether the
standardization of \code{x} needs a change-of-variables correction -- a density
reported in the simulator's units does, a ratio does not, since the Jacobian
cancels between its numerator and denominator.
}
\keyword{internal}
