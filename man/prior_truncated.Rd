% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_families.R
\name{prior_truncated}
\alias{prior_truncated}
\title{Truncate a prior to a box}
\usage{
prior_truncated(prior, lower = NULL, upper = NULL)
}
\arguments{
\item{prior}{An \code{nsbi_prior} from a named family.}

\item{lower, upper}{Truncation bounds. Numeric of length \code{prior$dim}, or
length 1 to apply the same bound to every parameter. Give one or both;
\code{-Inf}/\code{Inf} leaves that side alone.}
}
\value{
An \code{nsbi_prior} object.
}
\description{
Stan's \code{T[lower, upper]}, as an object. The returned prior is the original
restricted to the box and renormalized by the mass it keeps, so its
\code{log_prob} is a proper log density rather than the original shifted by an
unknown constant. That matters here in a way it does not in Stan: the
density is compared against a learned posterior in \code{\link[=nle]{nle()}}'s MCMC target and
summed with it in \code{\link[=c2st]{c2st()}} and the diagnostics, so a missing constant is a
wrong answer rather than a constant offset.
}
\details{
Renormalization needs the family's CDF, so \code{prior} has to come from a named
family: \code{\link[=prior_uniform]{prior_uniform()}}, \code{\link[=prior_normal]{prior_normal()}}, anything in \link{prior_families},
or a \code{\link[=prior_independent]{prior_independent()}} built from those. A \code{\link[=prior_custom]{prior_custom()}} is arbitrary
R code with no CDF behind it; give it \code{lower}/\code{upper} there instead, which
rejects out-of-support draws without claiming to renormalize.
}
\examples{
# A half-normal, the long way round (prior_half_normal() is the short one).
prior_truncated(prior_normal(mean = 0, sd = 1), lower = 0)

# A contact rate known to be between 0.1 and 2, log-normal in between.
prior <- prior_truncated(prior_lognormal(log(0.4), 0.5),
                         lower = 0.1, upper = 2)
range(sample_prior(prior, 100))
}
\seealso{
\link{prior_families}, \code{\link[=prior_independent]{prior_independent()}}, \link{priors}.
}
