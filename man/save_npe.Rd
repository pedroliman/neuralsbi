% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{save_npe}
\alias{save_npe}
\alias{load_npe}
\alias{save_nle}
\alias{load_nle}
\alias{save_nre}
\alias{load_nre}
\title{Save and reload a fitted model}
\usage{
save_npe(fit, path)

load_npe(path)

save_nle(fit, path)

load_nle(path)

save_nre(fit, path)

load_nre(path)
}
\arguments{
\item{fit}{An \code{nsbi_npe} object from \code{\link[=npe]{npe()}} or \code{\link[=npe_sequential]{npe_sequential()}}, an
\code{nsbi_nle} object from \code{\link[=nle]{nle()}}, or an \code{nsbi_nre} object from \code{\link[=nre]{nre()}}.}

\item{path}{File to write to (or read from). The convention is \code{.rds}.
\code{load_npe()} says so when there is no such file.}
}
\value{
\code{save_npe()} returns \code{path} invisibly. \code{load_npe()} returns the fit.
}
\description{
A fit whose estimator is \code{"maf"}, \code{"mdn"} or \code{"nsf"} holds a \code{torch} module,
and a torch module is an external pointer. \code{saveRDS()} writes the pointer,
not the network: the file reloads without complaint and the object prints
normally, but the first call that touches the network fails with
\verb{external pointer is not valid}. \code{save_npe()} and \code{load_npe()} are the
round trip that works.
}
\details{
\code{save_npe()} writes one \code{.rds} file holding the network's weights (via
\code{\link[torch:torch_save]{torch::torch_save()}} on its \code{state_dict}) alongside everything else the fit
carries as ordinary R objects: the prior, the standardization centers and
scales, parameter and outcome names, the simulation count, and the training
history. \code{load_npe()} rebuilds the network from the recorded architecture
and restores the weights, returning an \code{nsbi_npe} that behaves exactly like
the one you trained.

A \code{"linear_gaussian"} fit holds no torch objects and round-trips through
\code{saveRDS()} unharmed; \code{save_npe()} accepts it anyway, so saving code does
not have to know which estimator was used.

Weights are saved, not code. A fit saved by one version of \code{neuralsbi} loads
into a later one as long as the estimator's architecture has not changed;
\code{load_npe()} reports the version that wrote the file when the rebuild fails.

\code{save_nle()}/\code{load_nle()} and \code{save_nre()}/\code{load_nre()} are aliases; every
pair handles every kind of fit, and the extra names exist only so calling
code reads the way the fit was made.

A fit trained with \code{device = "cuda"}/\code{"mps"} (see \code{\link[=npe]{npe()}}/\code{\link[=nle]{nle()}}) always
reloads onto CPU, never onto the original device: \code{torch::torch_load()}
defaults to \code{device = "cpu"} regardless of where the tensors were saved
from, and \code{\link[=de_rebuild_net]{de_rebuild_net()}} builds the network fresh with no
\code{torch::with_device()} in effect. Move it back with
\code{fit2$de$net$to(device = "cuda")} if you want the reloaded fit on a GPU.
}
\examples{
prior <- prior_uniform(c(mu = -2, nu = -2), c(mu = 2, nu = 2))
simulator <- function(mu, nu) c(a = mu + rnorm(1, sd = 0.1),
                                b = nu + rnorm(1, sd = 0.1))
fit <- npe(prior, simulator, n_simulations = 500,
           density_estimator = "linear_gaussian")

path <- tempfile(fileext = ".rds")
save_npe(fit, path)
fit2 <- load_npe(path)
sample(posterior(fit2, x_obs = c(0.8, 0.6)), 100)
unlink(path)
}
