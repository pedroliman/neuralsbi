% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequential.R
\name{npe_sequential}
\alias{npe_sequential}
\title{Sequential NPE with truncated-prior proposals (TSNPE)}
\usage{
npe_sequential(
  prior,
  simulator,
  x_obs,
  n_rounds = 2L,
  n_simulations = 1000L,
  density_estimator = c("mdn", "maf", "nsf", "linear_gaussian"),
  epsilon = 1e-04,
  n_truncation_samples = 5000L,
  max_proposal_batches = 200L,
  seed = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{prior}{An \code{nsbi_prior} (see \code{\link[=prior_uniform]{prior_uniform()}}, \code{\link[=prior_normal]{prior_normal()}}).}

\item{simulator}{A function mapping an \verb{n x dim} matrix of parameters to an
\verb{n x d} matrix of simulated data.}

\item{x_obs}{The observation to target. Sequential inference concentrates
simulations around the posterior for this observation.}

\item{n_rounds}{Number of rounds. Round 1 is ordinary single-round NPE.}

\item{n_simulations}{Simulation budget per round; either a scalar or a
vector of length \code{n_rounds}.}

\item{density_estimator}{Passed to \code{\link[=npe]{npe()}} each round.}

\item{epsilon}{Mass cut for the truncation: the proposal region is the
\code{1 - epsilon} highest-probability region of the current posterior.}

\item{n_truncation_samples}{Posterior draws used to locate the truncation
threshold each round.}

\item{max_proposal_batches}{Cap on rejection-sampling batches per round.}

\item{seed}{Optional integer seed for reproducibility.}

\item{verbose}{Print per-round progress.}

\item{...}{Passed to \code{\link[=npe]{npe()}} (estimator and training settings).}
}
\value{
An object of class \code{c("nsbi_snpe", "nsbi_npe")} with a \code{rounds}
field recording per-round budgets, acceptance rates, and thresholds.
}
\description{
Multi-round NPE targeting a single observation \code{x_obs}. Single-round \code{\link[=npe]{npe()}}
spends its simulation budget across the whole prior; when only one
observation matters, most of those simulations land in regions the posterior
never visits. \code{npe_sequential()} implements truncated sequential NPE (TSNPE,
Deistler et al. 2022): after each round the prior is truncated to the
highest-probability region of the current posterior estimate, and the next
round's parameters are drawn from that truncated prior. Because every
proposal is proportional to the prior on its support, the standard NPE loss
stays valid -- no importance-weight or atomic correction is needed, which is
what makes TSNPE the simplest correct sequential scheme.
}
\details{
The rounds accumulate: each round's estimator is trained on all simulations
so far. The final fit is returned as an \code{nsbi_npe} (subclass \code{nsbi_snpe})
and works with \code{\link[=posterior]{posterior()}}, \code{\link[=sample]{sample()}} and the diagnostics, but unlike
single-round NPE it is \emph{not} amortized: it is only trustworthy at (or very
near) \code{x_obs}.

Proposal draws are obtained by rejection: prior candidates are kept when
their posterior log-density clears the \code{epsilon}-quantile threshold of the
current posterior's own draws. If the posterior is much narrower than the
prior the acceptance rate falls; the round then stops after
\code{max_proposal_batches} batches and continues with the draws it has,
with a warning.
}
\examples{
prior <- prior_normal(mean = c(0, 0), sd = 1)
simulator <- function(theta) theta + matrix(rnorm(length(theta), sd = 0.3),
                                            nrow = nrow(theta))
fit <- npe_sequential(prior, simulator, x_obs = c(0.5, -0.5),
                      n_rounds = 2, n_simulations = 1000,
                      density_estimator = "linear_gaussian")
post <- posterior(fit, x_obs = c(0.5, -0.5))
draws <- sample(post, 1000)
}
\references{
Deistler, Goncalves & Macke (2022), "Truncated proposals for
scalable and hassle-free simulation-based inference", NeurIPS.
\doi{10.48550/arXiv.2210.04815}
}
