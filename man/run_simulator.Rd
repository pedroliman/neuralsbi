% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{run_simulator}
\alias{run_simulator}
\title{Run a simulator over a parameter matrix}
\usage{
run_simulator(
  simulator,
  theta,
  sim_args = list(),
  label = "Simulating",
  d = NULL
)
}
\arguments{
\item{simulator}{The user's simulator, called once per row of \code{theta}.}

\item{theta}{Parameter matrix; its \code{colnames} are the parameter names used
to decide how the simulator receives its arguments.}

\item{sim_args}{Named list of extra arguments forwarded to every call.}

\item{label}{Phase name for the progress bar.}

\item{d}{Expected number of output columns, if known.}
}
\value{
An \verb{n x d} matrix, one row per row of \code{theta}.
}
\description{
The single point through which every simulator call in the package passes:
the per-parameter-set contract, the future backend, RNG streams, and the
progress bar all live here. See \link{nsbi_simulator} and \link{nsbi_parallel}.
}
\keyword{internal}
