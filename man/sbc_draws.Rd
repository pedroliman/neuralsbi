% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{sbc_draws}
\alias{sbc_draws}
\title{Shared simulate-and-drop preamble for sbc() and tarp()}
\usage{
sbc_draws(fit, simulator, prior, n, sim_args, what)
}
\arguments{
\item{fit}{An \code{nsbi_npe} or \code{nsbi_nle} fit.}

\item{simulator}{The simulator used for inference.}

\item{prior}{The prior to draw true parameters from.}

\item{n}{Number of trials to draw.}

\item{sim_args}{Named list of extra simulator arguments.}

\item{what}{Label for \code{\link[=drop_failed_sims]{drop_failed_sims()}}'s warning (e.g. \code{"SBC trials"}).}
}
\value{
A list with \code{theta} and \code{x} (the surviving trials), \code{n} (their
count, i.e. \code{nrow(theta)} after dropping), and \code{n_dropped}.
}
\description{
Both diagnostics draw "true" parameters from \code{prior}, simulate data for
them, and drop trials whose simulation failed. The prior-width check lives
here rather than in each caller, so \code{sbc()} and \code{tarp()} get it in one
place: \code{prior} exists to be overridden (see their docs), and a prior of the
wrong width would otherwise reach \code{sweep()}/z-scoring downstream and
silently recycle against the fit's width.
}
\keyword{internal}
