% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_flag}
\alias{check_flag}
\title{Validate a flag argument}
\usage{
check_flag(x, arg)
}
\arguments{
\item{x}{The user's value.}

\item{arg}{Name of the argument.}
}
\value{
\code{x}, unchanged.
}
\description{
One \code{TRUE} or \code{FALSE}. Branching on a flag with \code{isTRUE()} alone accepts the
value silently but only ever takes its "false" branch for anything that
is not the literal value \code{TRUE} -- \code{"yes"}, \code{1}, or \code{"TRUE"} as a string all
pass through and flip what the caller gets back with no error (#321).
\code{\link[=check_flag]{check_flag()}} rejects those up front so the mistake is caught at the call
that made it rather than read off a wrong-shaped result.
}
\keyword{internal}
