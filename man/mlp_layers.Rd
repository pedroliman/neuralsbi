% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flows.R
\name{mlp_layers}
\alias{mlp_layers}
\title{Stack of \verb{(linear, relu)} pairs, masked or plain.}
\usage{
mlp_layers(dims, masks = NULL)
}
\arguments{
\item{dims}{Layer sizes in order, e.g. \code{c(input, hidden1, hidden2, ...)}.}

\item{masks}{One weight mask per pair (as returned by
\code{made_masks()$hidden}) for an autoregressive trunk; \code{NULL} for a plain
MLP.}
}
\value{
A list of torch layers, ready for \code{do.call(torch::nn_sequential, .)}.
}
\description{
Each consecutive pair in \code{dims} becomes one \verb{(linear, relu)} step, matching
the trunk-building loop shared by the MADE, MDN and embedding networks.
}
\keyword{internal}
