% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nle.R
\name{nle}
\alias{nle}
\title{Neural Likelihood Estimation (NLE)}
\usage{
nle(
  prior,
  simulator = NULL,
  n_simulations = 1000,
  sim_args = list(),
  theta = NULL,
  x = NULL,
  density_estimator = c("maf", "mdn", "nsf", "linear_gaussian"),
  n_components = 10L,
  n_transforms = 5L,
  hidden = c(50L, 50L),
  n_bins = 10L,
  tail_bound = 3,
  max_epochs = 2000L,
  batch_size = 200L,
  lr = 5e-04,
  validation_fraction = 0.1,
  patience = 20L,
  n_restarts = 1L,
  clip_grad_norm = 5,
  standardize = TRUE,
  device = "cpu",
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{prior}{An \code{nsbi_prior} (see \code{\link[=prior_uniform]{prior_uniform()}}, \code{\link[=prior_normal]{prior_normal()}}).}

\item{simulator}{A function called once per parameter set, returning one
simulated observation: a numeric vector, a scalar, or a one-row matrix or
data frame. Parameters arrive either as named arguments (when the prior's
names match the simulator's formals) or as one named vector. Names on the
output become the outcome names used in plots. See \link{nsbi_simulator}.
Ignored if \code{theta} and \code{x} are given.}

\item{n_simulations}{Number of prior draws to simulate when \code{simulator} is
used and \code{theta}/\code{x} are not supplied.}

\item{sim_args}{Named list of extra arguments passed to every simulator
call: observed data, a time grid, a fixed population size, solver
settings. See \link{nsbi_simulator}.}

\item{theta, x}{Optional pre-computed simulations. If supplied, \code{simulator}
and \code{n_simulations} are ignored. Column names on \code{theta} (or names on
\code{prior}'s \code{mean}/\code{low}) and on \code{x} are carried through to posterior
samples, SBC results, and their plots.}

\item{density_estimator}{One of \code{"maf"} (Masked Autoregressive Flow, needs
\code{torch}; the default), \code{"mdn"} (Mixture Density Network, needs \code{torch}),
\code{"nsf"} (Neural Spline Flow, needs \code{torch}), or \code{"linear_gaussian"}
(closed-form baseline, no \code{torch}), or a function \verb{function(theta, x)}
returning a fitted estimator. Note the estimator sees the roles swapped:
its target is \code{x} and it conditions on \code{theta}.}

\item{n_components, hidden}{MDN settings: number of mixture components and a
vector of hidden-layer widths.}

\item{n_transforms}{MAF/NSF setting: number of stacked autoregressive
transforms.}

\item{n_bins, tail_bound}{NSF settings.}

\item{max_epochs, batch_size, lr, validation_fraction, patience}{Neural training
controls (Adam optimizer, early stopping on validation loss). The defaults
(\code{batch_size = 200}, \code{lr = 5e-4}, \code{validation_fraction = 0.1},
\code{patience = 20}) match Python \code{sbi}; \code{max_epochs} is a high guard cap that
early stopping normally reaches first.}

\item{n_restarts}{Train this many independently initialized networks and keep
the one with the best validation loss (guards against bad initializations
and MDN mode collapse).}

\item{clip_grad_norm}{Maximum gradient norm during training (\code{Inf} disables
clipping). The learning rate also decays 2x after 10 epochs without
validation improvement.}

\item{standardize}{Whether to z-score \code{theta} and \code{x} before training
(strongly recommended; default \code{TRUE}).}

\item{device}{Where to train the neural estimator: \code{"cpu"} (the default),
\code{"cuda"}, \code{"mps"}, or \code{"gpu"}/\code{"auto"} to resolve CUDA -> MPS -> CPU
(mirroring Python \code{sbi}'s \code{"gpu"}). CPU is the default on purpose --
matching \code{sbi}, not auto-selecting a GPU -- and \code{"cuda"}/\code{"mps"} error if
the requested device is not actually available rather than falling back
silently, so a typo or a missing driver is not mistaken for a slow CPU
run. Only \code{"gpu"}/\code{"auto"} falls back to CPU without complaint, since it
never named a specific device. Ignored (with no error) by
\code{"linear_gaussian"}, which has no GPU concept. For the small networks
typical of SBI models (the SIR example, say), \code{"mps"}/\code{"cuda"} can be
\emph{slower} than CPU -- per-kernel launch overhead dominates until the net
and batch are large -- so try CPU first and switch only if profiling
shows a win.}

\item{seed}{Optional integer seed for reproducibility.}

\item{verbose}{Print training progress.}
}
\value{
An object of class \code{nsbi_nle}. Evaluate the surrogate likelihood with
\code{\link[=log_lik]{log_lik()}}, turn it into a posterior with \code{\link[=posterior]{posterior()}}, or export it to
Stan with \code{\link[=stan_code]{stan_code()}}.
}
\description{
\code{nle()} trains a conditional density estimator on the \emph{other} factorization
of the joint: instead of the posterior \eqn{p(\theta \mid x)} that \code{\link[=npe]{npe()}}
learns, it learns a surrogate likelihood \eqn{q_\phi(x \mid \theta)}. The
posterior then follows from Bayes' rule,
\eqn{p(\theta \mid x) \propto q_\phi(x \mid \theta)\,p(\theta)}, and is
sampled with MCMC by \code{\link[=posterior]{posterior()}}.
}
\section{When NLE beats NPE}{


An NPE fit learns the posterior for one fixed data dimension, chosen at
training time. If the observation is \eqn{n} independent trials from the same
parameter, NPE must be retrained for every \eqn{n}, or handed summary
statistics that throw information away. NLE learns the density of a \emph{single}
trial, so the log-likelihood of \eqn{n} trials is a sum of \eqn{n}
evaluations:

\deqn{\log p(x_1, \ldots, x_n \mid \theta) = \sum_{i=1}^{n} \log q_\phi(x_i \mid \theta).}

Train once, then condition on 50 trials or 5000 without touching the network.
The learned likelihood is also a plain differentiable function of
\eqn{\theta}, so it can be embedded in a larger model written by hand --
see \code{\link[=stan_code]{stan_code()}}.

The trade-off is real and worth stating: posterior draws now cost an MCMC
run rather than a forward pass, and for a single fixed observation with
high-dimensional data NPE is usually the better choice.
}

\examples{
# One noisy measurement per simulator call; the observation is 200 of them.
prior <- prior_uniform(c(mu = -3, log_sigma = -1),
                       c(mu =  3, log_sigma =  1))
simulator <- function(mu, log_sigma) c(y = rnorm(1, mu, exp(log_sigma)))

fit <- nle(prior, simulator, n_simulations = 2000,
           density_estimator = "linear_gaussian")

x_obs <- matrix(rnorm(200, mean = 1, sd = 0.5), ncol = 1)
log_lik(fit, theta = c(1, log(0.5)), x = x_obs)
}
\references{
Papamakarios, G., Sterratt, D. and Murray, I. (2019). Sequential
Neural Likelihood: Fast Likelihood-free Inference with Autoregressive
Flows. \emph{AISTATS}. \doi{10.48550/arXiv.1805.07226}
}
\seealso{
\code{\link[=log_lik]{log_lik()}} and \code{\link[=likelihood_fn]{likelihood_fn()}} to evaluate the surrogate,
\code{\link[=posterior]{posterior()}} to sample it, \code{\link[=stan_code]{stan_code()}} to hand it to Stan.
}
