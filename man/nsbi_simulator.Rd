% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulator.R
\name{nsbi_simulator}
\alias{nsbi_simulator}
\title{The simulator contract}
\description{
A simulator in \code{neuralsbi} is called \strong{once per parameter set} and returns
\strong{one simulated observation}. Everything that calls a simulator --
\code{\link[=npe]{npe()}}, \code{\link[=simulate_for_sbi]{simulate_for_sbi()}}, \code{\link[=npe_sequential]{npe_sequential()}}, \code{\link[=sbc]{sbc()}}, \code{\link[=tarp]{tarp()}},
\code{\link[=posterior_predictive]{posterior_predictive()}} -- uses this contract.
}
\section{How parameters arrive}{


Two signatures are accepted, and which one applies is decided once per run
from \code{formals(simulator)}. If every parameter name appears among the
simulator's formals, the parameters are passed \strong{by name}, one scalar each:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{prior <- prior_uniform(low = c(mu = -5, sigma = 0.1),
                       high = c(mu =  5, sigma = 3))

simulator <- function(mu, sigma) \{
  y <- rnorm(100, mean = mu, sd = sigma)
  c(mean = mean(y), sd = sd(y))
\}
}\if{html}{\out{</div>}}

Otherwise the whole \strong{named parameter vector} goes to the first argument:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{simulator <- function(theta) \{
  y <- rnorm(100, mean = theta["mu"], sd = theta["sigma"])
  c(mean = mean(y), sd = sd(y))
\}
}\if{html}{\out{</div>}}

An unnamed prior always takes the vector form, and so does a prior whose
names are not syntactic R names (\code{"beta[1]"}), since those can never match a
formal.
}

\section{Everything else the simulator needs}{


Observed data, a time grid, a population size, a design matrix, a solver
tolerance: anything that is not a calibrated parameter goes in \code{sim_args}, a
named list forwarded to every simulator call.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{simulator <- function(alpha, beta, sigma, x_grid) \{
  rnorm(length(x_grid), mean = alpha + beta * x_grid, sd = sigma)
\}

fit <- npe(prior, simulator, n_simulations = 10000,
           sim_args = list(x_grid = seq(-1, 1, length.out = 50)))
}\if{html}{\out{</div>}}

A list is used rather than \code{...} because every one of \code{npe()}'s own
arguments would otherwise be exposed to R's partial matching: \code{x}, \code{theta},
\code{n} and \code{seed} are all natural names for a simulator argument and all of
them would be captured silently.
}

\section{What the simulator returns}{


One observation per call: a numeric vector of length \code{d}, a scalar, or a
one-row matrix or data frame. Names on the vector, or \code{colnames} on the
matrix or data frame, become the outcome names used in summaries and plots.
Every draw must agree on \code{d}. Lists, character or factor columns, and
anything with more than one row are rejected by name.

One sharp edge: arithmetic on a single named parameter keeps the name, so
\code{theta["beta"] / theta["gamma"]} returns a scalar named \code{beta} and would
quietly become an outcome called \code{beta}. Vectors are unaffected --
\code{theta["alpha"] + theta["beta"] * x} comes back unnamed. Use \code{unname()}, or
name the outcome deliberately with \code{c(r0 = ...)}.
}

\section{Failed simulations}{


A draw whose output contains \code{NA}, \code{NaN} or an infinite value is dropped,
together with its parameters, and one warning per run reports the count and
the rate. A single non-finite value would otherwise poison the training loss
for the whole fit and surface much later as a \code{NaN} validation loss.

Dropping conditions on the simulator having succeeded. When failure depends
on the parameters -- an ODE that diverges for large \code{beta}, a model that
returns \code{NA} outside a stability region -- the surviving draws are no longer
a sample from the prior, and the fit targets the posterior \emph{given success}
rather than the posterior. A high drop rate is a modelling signal, and the
honest fix is usually to handle the failure inside the simulator.
}

\seealso{
\link{nsbi_parallel} for how the calls are spread across workers.
}
