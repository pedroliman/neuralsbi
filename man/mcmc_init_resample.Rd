% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmc_init_resample}
\alias{mcmc_init_resample}
\title{\code{"resample"} starting points: pool prior draws, then SIR without replacement}
\usage{
mcmc_init_resample(prior, log_prob_fn, n_chains, n_pool = 1000L)
}
\description{
A single pool of \code{max(n_pool, n_chains)} prior draws can land fewer than
\code{n_chains} of them in the posterior's support -- the same shape of problem
\code{\link[=mcmc_init_proposal]{mcmc_init_proposal()}} fixed for \code{"proposal"}. Padding the shortfall by
recycling (\code{rep_len()} on too few indices) used to start several chains from
identical points, which defeats the diagnostics downstream: split-Rhat and
bulk ESS both assume the chains they compare started from distinct
locations, and a repeated start makes chains look more agreed than they are.
}
\details{
Instead, more pools are drawn and their finite draws accumulated until
\code{n_chains} distinct ones are collected (up to 20 attempts, the same budget
\code{\link[=mcmc_init_proposal]{mcmc_init_proposal()}} uses), and the final start is a weighted resample
without replacement from that accumulated, still-distinct set -- not from a
single pool. If the budget runs out first, the error distinguishes a
genuinely degenerate surrogate (no draw at all landed in support) from a
merely low acceptance rate (some did, just not \code{n_chains} of them).
}
\keyword{internal}
